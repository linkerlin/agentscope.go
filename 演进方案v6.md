# AgentScope (Python) vs AgentScope.Go 对比与演进方案 v6

> **版本**：2026-08-21（基于 Python main 分支 801dd1ef / Go v2.6.0 commit 9ae7158）
> **修订背景**：v5 方案 Phase 14 已全部落地并推送（Console TUI / Workspace 服务化 / 治理-演化闭环 / 多租户隔离）。Python 在同期（0d54503e→801dd1ef）又完成 **36 个非 merge 提交**，主题从"平台化"转向"**可观测性 + 工具精细化 + Agent Team 增强**"。本版基于 2026-08-21 双方真实源码重新审阅。

---

## 0. 核心结论（先读）

1. **v5 Phase 14 全部兑现**（v2.6.0）：Console TUI、Workspace 服务化、治理-演化闭环、多租户会话隔离（安全修复）、文档同步。
2. **新一轮差距已形成**（36 提交，其中 10 个 feat）。经逐项验证，真实差距收敛为：
   - 🔴 **KB 可观测性**（#2372）：chunk 浏览 + 原始文档预览 + 富化列表 —— Go 仅有 chunk 计数。
   - 🟠 **工具三件套**：read_file 二进制 DataBlock（#2114）、FunctionTool 自定义 schema（#2378）、Bash 按后端 OS 选 shell（#2366）。
   - 🟠 **on_reply 续循环语义**（#2322）：中间件可吞 ReplyEndEvent 让回复继续。
   - 🟡 **Team/上下文精细化**：失败通知 leader（#2386）、loop 中间件（#2379）、max_image_num（#2362）、prompt cache tokens（#2318）。
3. **已对齐无需动作**：表格转义（#2377，Go parser 已有 `|` 转义）、Channel 专用 worker（#2390/#2395，Go Dispatcher 模型等价）、chunker 配置化（#2083，Go 已参数化）。
4. **Go 护城河待放大**：evolver 真实 MCP 后端 e2e、治理指标导出、benchmark 权威报告。

---

## 1. Python 36 提交分类与 Go 现状（已验证）

| 类别 | 提交 | Go 现状 | 动作 |
|------|------|---------|------|
| KB 可观测性 | #2372 chunk 浏览/文档预览/富化列表 | kb_handlers 仅 Chunks 计数 | 🔴 P0 |
| 工具增强 | #2114 Read DataBlock（二进制/结构化） | tool/file 纯文本 | 🟠 P1 |
| 工具增强 | #2378 FunctionTool 自定义 input_schema | 仅 ExtractInputSchema[T] 自动生成 | 🟠 P1 |
| 工具修正 | #2366 Bash 按后端 OS 选 shell | Local workspace Execute 待查证 | 🟠 P1 |
| 中间件语义 | #2322 on_reply 吞 ReplyEndEvent 续循环 | Reply 洋葱单次返回，无续循环 | 🟠 P1 |
| Team | #2386 成员失败通知 leader | wakeup 仅成功 inbox drain | 🟡 P2 |
| Team | #2379 子 agent loop 中间件 | 无 | 🟡 P2 |
| 上下文 | #2362 max_image_num | 无 | 🟡 P2 |
| usage | #2318 prompt cache tokens | 无 | 🟡 P2 |
| UI | #2381 超迭代警告 | web_ui 无 | 🟡 P2 |
| fix 批次 | #2311/#2308/#2349/#2350/#2351 等 | 需逐项核对 | 🟡 P2 |

## 2. Phase 15 方案（目标 v2.6.1）

### Phase 15.1：KB 可观测性（P0）✅ 已完成（2026-08-21）

- `GET /api/v1/knowledge-bases/{id}/documents/{doc_id}/chunks`：分页浏览已索引 chunk（内容 + metadata，vector 剥离，按 chunk_index 排序）
- `GET /api/v1/knowledge-bases/{id}/documents/{doc_id}/raw`：原始文档预览（worker 索引时把 blob_uri 注入 chunk metadata → 溯源 blob 流式返回 + 嗅探 Content-Type）
- KB 列表富化：document_count/chunk_count 聚合
- web_ui KB 详情页：文档行"分块"懒加载展开 + "原文"链接
- 实现：`VectorStore.ListChunks` 接口 + InMemory 实现 + `KnowledgeBase.ListChunks`（filter 强制多租户隔离）；3 组测试全绿

### Phase 15.2：工具三件套（P1）✅ 已完成（2026-08-21）

- **read_file DataBlock**：非有效 UTF-8 内容 → base64 `DataBlock`（TypeData + http.DetectContentType 嗅探 media_type）+ 说明文本块；文本文件行为不变（2 测试）
- **WithInputSchema 覆盖**：`FunctionToolOption` 新增 `WithInputSchema(schema)`，覆盖自动生成的参数 schema（手调 description/enum/format 提升模型准确度），typed handler 不受影响（1 测试）
- **Bash 按后端 OS 选 shell（#2366）**：核查结论——**Go 架构无此问题**。Go 的 shell 工具透传命令字符串，shell 选择由各 Workspace 后端自行决定（Docker/K8s 在容器内用 `sh -c`，Local 即宿主），与 Python 在工具层按 host OS 包装的架构不同。无需动作

### Phase 15.3：on_reply 续循环语义（P1）✅ 已完成（2026-08-21）

Go 化设计（对齐 #2322 "中间件吞 ReplyEndEvent 续跑"）：
- `middleware.ErrContinueReply` 哨兵：OnReply 返回它即请求再跑一轮
- `Base.Call` 循环重入洋葱链：每轮把中间回复追加进 `ReplyInput.Messages`（供中间件判定），上界 3 轮防死循环（超界保留最后一轮回复）
- 向后兼容：不返回哨兵则行为与之前完全一致
- 测试：续跑一轮（reply 执行 2 次）/ 永续被上界截断（4 次）/ 错误透传

### Phase 15.4：Team/上下文精细化 + fix 批次（P2）🔶 部分完成（2026-08-21）

- ✅ **worker turn 失败通知 leader**（#2386）：`watchRunFailure` 消费 run 事件流检测 ErrorEvent → `notifyLeaderOfFailure` 把 `<team-error>` 推入 leader inbox + wakeup（300 字符截断、leader 自身/无 team 时不通知）。测试全绿
- ⬜ max_image_num（#2362）：需动 react 上下文组装链，延后
- ⬜ loop 中间件（#2379）/ prompt cache tokens（#2318）/ 超迭代 UI（#2381）/ MCP SSE 探测与重连（#2311/#2308）：低价值密度，按需触发

### 护城河（并行）

- ✅ **治理指标导出**：`observability.ControlPlaneCollector`（9 个 `agentscope_controlplane_*_total` counter，scrape 时拉快照零后台 goroutine）+ `Server.WithMetricsRegistry(reg)`（挂 controlPlane 自动注册 + `/metrics` 路由）；examples/controlplane_http 实测输出
- ⬜ `examples/evolver` 升级：MCPEvolver 连真实后端 e2e 脚本（需真实 Evolver MCP 服务）
- ✅ **v2.6.0 性能快照**：`docs/benchmark_v2.6.0.md`——gateway 147k req/s（内存管线）/2.7k（真 TCP）、1→100 并发延迟仅 +27%、ReplyStream ~4.4ms/2.1MB、热路径零/低分配（限流 0 allocs）

---

## 3. 优先级与执行顺序

```
P0（立即）  15.1 KB 可观测性（chunk 浏览/raw 预览/富化列表）
P1（次优）  15.2 工具三件套 → 15.3 on_reply 续循环
P2         15.4 Team/上下文/fix 批次
并行始终    evolver e2e + 治理指标导出 + 性能报告
```

## 4. 风险

| 风险 | 缓解 |
|------|------|
| InMemoryVectorStore 无 chunk 枚举 API | 增加带索引的 doc→chunks 聚合（内存成本低） |
| on_reply 续循环破坏现有洋葱契约 | 哨兵错误方案向后兼容（不返回即终止） |
| DataBlock 嗅探误判 | 仅对不可解码 UTF-8 的内容走 base64 分支 |

## 5. 验收指标

| 指标 | 当前 v2.6.0 | 目标 v2.6.1 |
|------|------------|-------------|
| KB chunk 浏览/预览 | 仅计数 | ✅ 3 端点 + web_ui 展示 |
| read_file 二进制 | 不支持 | ✅ DataBlock base64 |
| FunctionTool 自定义 schema | 不支持 | ✅ WithInputSchema |
| on_reply 续循环 | 无 | ✅ ErrContinueReply 哨兵 + 3 轮上界 |
| Team 失败通知 | 无 | ✅ leader inbox `<team-error>` |
| `go test ./... -race` | 93 包全绿 | 全绿 |
