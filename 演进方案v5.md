# AgentScope (Python) vs AgentScope.Go 对比与演进方案 v5

> **版本**：2026-08-21（基于 Python main 分支 0d54503e / Go v2.5.1 commit 743ef11）
> **修订背景**：v3/v4 方案规划的 Phase 5-13 已全部落地（v2.5.0），且 Go 在 08-09~11 又**超纲自发演进**出 LoopX controlplane 治理平面与 react 生产加固（v2.5.1）。本版基于 2026-08-21 双方真实源码重新审阅，并对上一轮演进分析做了事实复核（修正 4 处错误归因，见 §2.3）。

---

## 0. 核心结论（先读）

1. **演进方案 v3/v4 已 100% 兑现**：Phase 5-13（RAG KB / CoordBus / Web UI / Agentic Memory / Workspace 7 后端 / SQL 存储 / 7 钩子 / Injection / PowerShell / Word+Excel / Access Policy / Channel 三平台 / Hub 市场）全部完成。
2. **Go 已超纲演进出一轮 Python 完全空白的能力**：`controlplane/`（LoopX 风格治理平面：goals/gates/quota/evidence/leases/rewards/kanban，6 轮落地）+ react 加固（断路器/steer-abort/MaxTurnDuration/ToolResultScreener）+ 会话级权限审批。**这是当前最大的反向护城河。**
3. **v4 基准（6cfd9f0）之后 Python 仅 6 个实质提交**，真实新差距收敛为两个方向：
   - 🔴 **Console 终端**（#2297）：终端 REPL 调试入口，唯一全新维度。
   - 🟠 **Workspace 服务化深化**（#1951 共享 + #2283 skills 隔离/选择）。
4. **复核发现 v4 方案自身漏盘点了三个存量差距**（v4 基准前已存在但未列入）：workspace artifact 端点（#2187）、web-ui 工作目录+git status（#2257）、中断原因保留（#2209）。本轮一并纳入。
5. **误报剔除**：hub 环境输入（Go `ServerSpec.Env` 已透传，无差距）；Gemini `$schema`（Go 全仓库零命中，不受影响）。
6. **AGENTS.md 严重过时**：版本号仍写 v2.4.0，且未记录 controlplane/react 加固等 6 轮工作，需全面更新。

---

## 1. 执行盘点：v3/v4 方案完成度

| 方案 | Phase | 状态 | 落地版本 |
|------|-------|------|----------|
| 演进方案.md（v3） | Phase 5-11 | ✅ 全部完成 | v2.4.0~v2.4.3 |
| 演进方案v4.md | Phase 12 Channel 三平台 + Phase 13 Hub | ✅ 完成 | v2.5.0 |
| 超纲自发 | LoopX controlplane（6 轮）+ react Q8/Q13 加固 + 会话级审批 | ✅ 完成 | v2.5.1 |

**遗留**（来自 TODO.md，均为可选项）：Apple Container workspace、ES/MongoDB 向量库、S3 BlobStore、NATS 后端、K8s Operator、ChatService 编排提炼、gateway 多租户 SSE/WS 集成测试、docs/WORKSPACE.md。

---

## 2. 对照最新 Python（0d54503e）的差距分析

### 2.1 v4 基准之后的真实新差距（`git rev-list 6cfd9f0..HEAD` = 7 commits，6 个实质）

| # | Python 提交 | 内容 | Go 现状 | 差距级别 |
|---|------------|------|---------|----------|
| 1 | **#2297 console** | `src/agentscope/console/`（165 行）：stdin 多轮 REPL + 三档 verbosity 渲染全部事件 + HITL 工具确认（y/n/a，a 接受 suggested_rules）+ Ctrl+C 转中断 + 无持久化 | 无 | 🔴 P0 唯一新维度 |
| 2 | **#1951 workspace 共享** | workspace 可跨不同 agent 共享 | `WorkspaceManager` 按 (user,agent,session) 键控，无共享语义 | 🟠 P1 |
| 3 | **#2283 skills 隔离/选择** | skills 按 agent 隔离存储 + 按 session 选择启用 | skills 仅 session 级（`SessionWorkspace.Skills`） | 🟠 P1 |
| 4 | #2230 hub env inputs | UI 表单安装 GitHub MCP 时保留环境输入 | **数据通路已支持**（`ServerSpec.Env` 透传），仅 UI 层无表单 | 🟢 已对齐（误报剔除） |
| 5 | #2295 inbox 滞留修复 | session inbox payload 不再滞留到下一轮 | wakeup_dispatcher 已有 inbox drain | 🟢 已对齐 |
| 6 | #2277 gemini $schema | 移除工具参数中不受支持的 $schema | 全仓库 `$schema` 零命中 | 🟢 不受影响 |

### 2.2 v4 方案漏盘点的存量差距（基准前已存在，本轮补入）

| # | Python 提交 | 内容 | Go 现状 |
|---|------------|------|---------|
| 7 | **#2187 artifact 端点** | workspace artifact `list_dir`/`read_file` HTTP 端点 | gateway 仅 `/workspace/mcp|skill` 管理路由，无文件端点 |
| 8 | **#2257 工作目录+git status** | web-ui 显示 session 工作目录与 git 状态 | web_ui 无 |
| 9 | #2209 中断原因保留 | 中断时保留部分响应原因 | react 返回固定恢复文案（`react_agent.go:1111`） |

### 2.3 上一轮分析的复核修正记录

1. ~~"v4 后 15+ commits"~~ → 实际 7 个（6 实质）。
2. ~~console"含输入补全"~~ → 源码为裸 `input()`，无补全；真实语义见 §2.1#1。
3. ~~"Go hub 不支持环境输入"~~ → `toolkit/mcp/catalog.go:20` `ServerSpec.Env` 存在且 `InstallMCPs` 透传，误报剔除。
4. ~~#2187/#2257/#2209 列为"新增差距"~~ → 均在 v4 基准之前，属 v4 漏盘点的存量差距。

---

## 3. Go 护城河与深化方向（只补不攻的"攻"）

| 护城河 | 现状 | 深化方向 |
|--------|------|----------|
| **controlplane 治理平面** | goals/gates/quota/evidence/leases/rewards/kanban + HTTP + web_ui 视图 | **validated-writeback 闭环**：controlplane 证据校验通过后自动触发 `evolver.Solidify`（治理复盘 → GEP Gene/Capsule 固化）；跨 agent 目标委托（leader goal 分解给 worker） |
| react 生产加固 | 断路器/steer-abort/MaxTurnDuration/ToolResultScreener/会话级审批 | web_ui 控制台增加"打断/注入"操作入口；断路器阈值配置化暴露 |
| A2A / ReMe / ONNX / Plugin | 持续领先 | 维持，不稀释 |

---

## 4. Phase 14 演进方案（目标 v2.6.0，2026-Q4）

### Phase 14.1：Console 终端（P0）✅ 已完成（2026-08-21，采用 bubbletea/bubbles TUI）

> 对标 Python `src/agentscope/console/`（#2297）。按评审意见采用 charmbracelet **bubbletea + bubbles + lipgloss** TUI（替代纯 stdin 方案）。Go 侧 HITL/中断原语已齐全（`event.RequireUserConfirmEvent`/`NewUserConfirmResult`/`V2Agent.InjectEvent`/`SessionStateManager.Resume`）。

#### 14.1.1 `console/` 包
- `console/console.go`：`Launch(ctx, agent, opts)` —— stdin 多轮循环（`exit`/`quit`/Ctrl+D 退出）
- 事件渲染：消费 `ReplyStream()` channel，三档 verbosity（quiet/default/debug），工具结果按行数截断
- HITL：收到 `RequireUserConfirmEvent` → 逐个工具调用询问 `[y]es/[N]o/[a]lways` → `InjectEvent(NewUserConfirmResult(...))` 恢复
- 中断：Ctrl+C（`os/signal`）→ 取消当前 reply + 触发 agent 中断路径
- 无会话持久化（对齐 Python：state 存于 agent，随进程结束）

#### 14.1.2 示例与测试
- `examples/console/`：可运行 demo（llmenv 配置 + 带权限工具的 ReAct agent，演示 HITL 确认流）
- `console/console_test.go`：事件渲染/确认注入/退出条件（用 stdin pipe + mock agent）

**验收**：`go run ./examples/console` 可多轮对话；工具确认 y/n/a 三路径通过；Ctrl+C 中断不悬挂。

### Phase 14.2：Workspace 服务化（P1）✅ 已完成（2026-08-21）

> 合并 v4 后新差距（#1951/#2283）与 v4 漏项（#2187/#2257）。落地方式：artifact/status 只读端点（safeJoin 防穿越 + 5MiB 上限 + git CLI 优雅降级）；共享工作区复用已有 `Session.WorkspaceID` 字段（零 schema 改动，`baseRoot/user/shared/<name>`，路径段消毒）；skills 升级为 agent 级库 + session `SelectSkills` 白名单选择（向后兼容）。

#### 14.2.1 artifact 文件端点（对标 #2187）
- `gateway/workspace_handlers.go` 新增：
  - `GET /api/v1/sessions/{id}/workspace/list_dir?path=`
  - `GET /api/v1/sessions/{id}/workspace/read_file?path=`
- 只读 + requireAuth + 路径穿越防护（复用 `hub.safeJoin` 同款语义）

#### 14.2.2 工作目录 + git status（对标 #2257）
- `GET /api/v1/sessions/{id}/workspace/status`：返回工作目录路径 + `git status --porcelain` 摘要（exec git CLI，非仓库/无 git 优雅降级为空）
- web_ui Chat 视图顶部显示工作目录与变更文件数

#### 14.2.3 workspace 跨 agent 共享（对标 #1951）
- `WorkspaceManager` 增加命名共享工作区：`GetOrCreate` 支持 `shared=<name>` 解析到 `WorkspaceBaseDir/shared/<name>`，多 (agent,session) 挂同一目录
- HTTP：`POST /workspace` 带 `shared` 参数；文档说明隔离边界

#### 14.2.4 skills agent 级隔离 + session 选择（对标 #2283）
- agent 级 skill 库：`Storage` 持久化 agent→skills 归属（复用 RBAC OrgID 隔离）
- session 选择：`SessionWorkspace.Skills` 从 agent 库按白名单装载（默认全量，向后兼容）
- HTTP：`GET/POST /api/v1/agents/{id}/skills`、session 创建时 `skill_names` 过滤

**验收**：4 组端点集成测试全绿；web_ui 可见工作目录/git 状态；共享目录双 agent 读写互通；session 只看到所选 skills。

### Phase 14.3：治理-演化闭环（P1，Go 独有方向）✅ 已完成（2026-08-21）

- `controlplane` ↔ `evolver` 接线：Goal 完成且证据校验通过（validated writeback）→ 自动 `evolver.SolidifyRequest`（DecisionSource=controlplane，PrimaryCause=goal_id）
- 配置开关 `AppConfig.AutoSolidifyOnGoalComplete`（默认关）
- web_ui control-plane 视图增加"打断/注入"操作入口（复用 67fc867 steer/abort 能力）
- 示例：`examples/controlplane_demo` 增加"goal 完成 → capsule 生成 → 二次复用"闭环演示

**落地细节**：goal→completed 状态迁移只在 `handleUpdateCPGoal` 一处发生，hook 置于迁移成功之后（防重复 PATCH 重复固化）；异步 goroutine + recover 防护（外部 Evolver 实现 panic 不击穿服务）；顺带修复 `MockEvolver.Solidify` 对 nil Gene 的 panic（合法输入）。web_ui 为 **steer/打断** 端点（`POST /v2/sessions/{id}/steer|interrupt`，挂 SessionManager 活动 run，agent 类型断言优雅降级）。

### Phase 14.4：文档与遗留清理（P2）✅ 已完成（2026-08-21）

- **AGENTS.md 全面更新**：版本号 v2.4.0 → v2.5.1（Phase 14 目标 v2.6.0）；架构总览补录 controlplane 治理层、console 终端层；代码量表格 +2 模块（~71,100 非测试行）；关键设计决策 47-48 条（TUI Console / 治理-演化闭环 + steer-abort + 多租户隔离）
- `docs/WORKSPACE.md`：7 后端使用指南（接口/场景/构造）+ 会话工作区服务化（artifact/status/skills 选择/共享工作区）+ HTTP 端点表
- **gateway 多租户 SSE/WS 集成测试**（旧 Phase 1 遗留）：补齐用户级会话隔离——**发现并修复真实安全缺口**：v2 的 POST/GET/DELETE/WS 原先不校验 session 归属（认证后任意用户可订阅/终止他人 run），新增 `checkSessionAccess`（storage 可用时跨用户一律 404，不泄露存在性；无 storage 单用户模式放行），4 个插入点 + 2 组集成测试
- 中断原因保留（#2209）：react 恢复文案附带被截断的 partial response 摘要（`lastAssistantText` + 80 rune 上限，无 partial 时保持原文案）
- 明确不做/延后：Apple Container、ES/MongoDB、S3、NATS、K8s Operator（外部依赖，按需触发）

---

## 5. 优先级与执行顺序

```
P0（立即）  14.1 Console 终端          ← 唯一新维度，原语齐全，~200 行薄封装
P1（次优）  14.2 Workspace 服务化      ← artifact 端点 → git status → 共享 → skills
            14.3 治理-演化闭环         ← 放大 controlplane+evolver 双护城河
P2         14.4 文档/遗留清理         ← AGENTS.md 更新 + 集成测试 + WORKSPACE.md
并行始终    守护 A2A / ReMe / ONNX / Plugin，禁止只补不攻
```

> 建议从 **14.1（Console）** 起步——依赖最少、见效最快，且与既有 HITL/中断原语天然契合；14.2.1（artifact 端点）紧随其后（web_ui 已需要）。

---

## 6. 关键风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| Console Windows 下 Ctrl+C 信号处理差异 | 中 | 低 | `os/signal.Notify` 跨平台统一；Windows 回退为 KeyboardInterrupt 风格退出 |
| artifact 端点路径穿越 | 低 | 高 | 强制 safeJoin（拒绝绝对路径/`..`），只读，requireAuth |
| 共享 workspace 并发写冲突 | 中 | 中 | 首版文档声明"共享目录由使用方协调"；文件工具已有原子写 |
| skills agent 级存储破坏现有 session 语义 | 低 | 高 | 默认全量装载（向后兼容），白名单为 opt-in |
| controlplane→evolver 自动固化产生噪声 capsule | 中 | 中 | 默认关闭（opt-in）+ Solidify 带 DecisionSource 可审计 |

---

## 7. 验收指标（Phase 14）

| 指标 | 当前（v2.5.1） | 目标（v2.6.0） |
|------|---------------|----------------|
| Console 终端 | 无 | ✅ `console/` 包 + examples/console（bubbletea TUI，HITL/中断/三档渲染，8 测试全绿） |
| workspace 文件端点 | 无 | list_dir + read_file（只读+防穿越） |
| 工作目录/git status | 无 | 端点 ✅（web_ui 显示待示例具备会话/认证管线后接入） |
| workspace 共享 | 无 | ✅ 命名共享工作区（复用 Session.WorkspaceID） |
| skills 隔离/选择 | session 级 | ✅ agent 级库 + session 白名单选择 |
| 治理-演化闭环 | 无 | ✅ goal 完成 → 自动 solidify（opt-in） |
| AGENTS.md | v2.4.0（过时） | ✅ 更新至当期架构（v2.5.1 / 165 包） |
| `go test ./... -race` | 全绿 | 全绿 |

---

## 8. 结语

v3/v4 方案兑现后，Go 版已通过 controlplane + react 加固建立起 Python 没有的治理层护城河。本轮（Phase 14）差距极小且收敛：**Console 终端是唯一新维度**，Workspace 服务化是既有方向的深化，其余为 v4 漏盘点的存量小项。执行重心应为"一个 P0 薄封装（Console）+ 一组服务化端点（Workspace）+ 一次护城河闭环（治理→演化）"，同时把过时的 AGENTS.md 补齐——让文档与代码重新同步。
