import "./decision.css";
export interface CheckpointProps {
    /** What the human needs to confirm / decide. Required. */
    question?: string;
    /** Optional choices presented to the human. */
    options?: string[];
}
/** A human-in-the-loop confirmation point. */
export declare function Checkpoint({ question, options }: CheckpointProps): import("react").JSX.Element;
//# sourceMappingURL=Checkpoint.d.ts.map