import type { ReactNode } from "react";
import "./index.css";
export declare const THEMES: readonly ["tufte", "press", "shannon", "vignelli", "knuth", "freddie", "andy", "bodoni", "bayer", "fuller", "sottsass"];
export type ThemeName = (typeof THEMES)[number];
export declare const THEME_LABELS: Record<ThemeName, string>;
export interface ThemeProviderProps {
    /** Which visual theme to apply. AI/authors only pick a theme, never styles. */
    theme?: ThemeName;
    children: ReactNode;
    className?: string;
}
/**
 * Establishes the ReActicle styling root. Sets `data-theme` so every nested
 * component resolves its `var(--ra-*)` tokens against the chosen theme.
 */
export declare function ThemeProvider({ theme, children, className, }: ThemeProviderProps): import("react").JSX.Element;
//# sourceMappingURL=ThemeProvider.d.ts.map