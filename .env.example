# LLM API 配置模板：复制为 .env（或直接导出到环境变量）后运行示例。
# 环境变量优先于 .env 文件；从任意示例目录运行均可（会向上查找 .env）。
OPENAI_API_KEY=sk-xxx
# 可选：OpenAI 兼容网关地址（DashScope 兼容模式 / vLLM / OneAPI 等）
#OPENAI_BASE_URL=https://api.openai.com/v1
# 可选：模型名，默认 gpt-4o-mini
#OPENAI_MODEL=gpt-4o-mini
