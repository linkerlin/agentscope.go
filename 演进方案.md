# AgentScope (Python) vs AgentScope.Go 深度对比与演进方案 v3

> **版本**：2026-07-30 深度修订版（取代 2026-07-03 v2）
> **基准快照**：
> - Python 版：`C:/GitHub/agentscope/` main 分支（2026-07-30 工作区），AgentScope **2.0.5**
> - Go 版：`C:/GitHub/agentscope.go/` **v2.4.0**（2026-07-30 工作区）
>
> **修订背景**：v2 方案（2026-07-03）制定了 Phase 5-8 路线并已全部落地（RAG 托管 KB、CoordBus、Web UI、Agentic Memory）。但 **Python 版在最近 5 周（06-25 至 07-30）又完成了一轮"沙箱多元化 + 存储深化 + 中间件精细化"迭代**（67 commits），新增了 5 个 Workspace 后端、2 个 RAG 解析器、2 个向量库后端、SQL 存储后端、运行时状态注入、权限中间件钩子、PowerShell 工具等，再次拉开了新的差距。本版基于 **2026-07-30 双方真实源码**重新深度审阅，在肯定 Go 版 Phase 5-8 成果的同时，聚焦 Python 新增领先维度，制定精准的下一阶段演进路线。

---

## 0. 核心结论（先读这段）

1. **Go 版在"分布式 + 性能 + 前沿特性"上仍然领先**：A2A 协议、ReMe 记忆、GEP 自演化、ONNX 推理代理、Plugin 系统、SQLiteVec、断路器、DAG 执行器、Streamable HTTP —— 这些是 Go 独有或反超的能力，**必须继续放大**。
2. **Phase 5-8 成果显著**：RAG 托管 KB（4 parser + chunker + blob + KB + index + middleware + HTTP API）、CoordBus 四原语、零构建 Web UI、Agentic Memory、Tracing 语义提取 —— 上一轮识别的 TIER 1-3 差距已实质补齐。
3. **Phase 9-11 快速跟进**：Python 最近 5 周新增的差距已大部分补齐——
   - **Workspace 沙箱**：3→**7** 后端（+K8s/Bubblewrap/Daytona/OpenSandbox），仅差 Apple Container。
   - **SQL 存储**：新增 SQLite 后端（纯 Go 零 CGO + 级联删除）。
   - **中间件精细化**：5→**7** 钩子（+on_check_permission/on_compress_context）。
   - **运行时注入**：新增 InjectionMiddleware（自动时间/extra_fields）。
   - **RAG 扩展**：4→**6** parser（+Word/Excel）。
   - **工具/策略**：新增 PowerShell 专用工具 + 资源跨用户共享策略。
4. **剩余可选差距**：Apple Container workspace（macOS only）、ES/MongoDB 向量库实现（替换占位）、S3 BlobStore、PostgreSQL 方言。均为"可选/需外部依赖"性质，不影响核心功能完整度。

---

## 1. 维度化深度对比（2026-07-30 真实源码）

> 下表基于对双方 `src/` 与根目录的逐文件审阅。代码量按非测试/测试行近似估算。**★** 标记本轮（v2→v3）新增或变化的维度。

| 维度 | Python 2.0.5（07-30） | Go v2.4.0 | 领先方 | 真实差距说明 |
|------|----------------------|-----------|--------|--------------|
| **RAG：托管知识库服务** | **完整**：parser(PDF/PPT/Image/Text/**Word**★/**Excel**★) + Chunker + VDB(Milvus/Qdrant/**MongoDB**★/**Elasticsearch**★) + `app/rag`(BlobStore Local/S3 + KBManager + IndexWorker + IndexSweeper) + KB HTTP Router + `RAGMiddleware`(29KB) | **已补齐基础**：rag/{document,parser(4),chunker,blob,kb,index} + RAGMiddleware + KB HTTP API | **Python 仍领先** | Go 缺 Word/Excel 解析器、MongoDB/ES 向量库（占位）、S3 BlobStore |
| **Agent 服务编排层 `app/`** | **完整**：`create_app()` 工厂 + 9 个 Router + ChatService + BackgroundTaskManager + WakeupDispatcher + CancelDispatcher + SchedulerManager + **ResourceAccessPolicy**★ | **已补齐**：`NewApp` + `AppConfig` + `RegisterAppRoutes` 工厂 + wakeup/offload/team | **Python 略领先** | Go 缺跨用户资源共享策略 |
| **消息总线** | **领域无关完整**：MessageBus base + InMemory + Redis + Keys；publish/subscribe/queue/registry/lock/projection 全套原语 | **已补齐**：CoordBus 四原语(Lock/Registry/Queue/Log) Local+Redis + 跨会话投影 | **基本持平** | — |
| **Web UI** | **完整 React SPA**：React19+Vite+Tailwind+shadcn★+i18n | **零构建 SPA**：vanilla JS + SSE + go:embed（Chat/KB/System 三视图） | **Python 领先** | Python UI 更丰富(shadcn 组件库、i18n)，Go 版够用但简朴 |
| **记忆系统** | ReMe/mem0/AgenticMemory + **用户自定义 hints 压缩**★ | ReMe(文件/向量/Hybrid/Dream/KG) + AgenticMemory + LongTermMemory | **Go 领先（功能量）** | Go 缺用户自定义 hints 保留机制 |
| **多 Agent 编排** | Msg 列表 + MultiAgentFormatter + Agent Team 异步协作 | Pipeline/MsgHub/Workflow/Reflection/MapReduce + Team 同步/异步双模 | **Go 领先** | — |
| **A2A 协议** | Roadmap 提及，无实现 | 完整(AgentCard/Task/SSE/Registry/ShardRouter/ClusterManager/安全/Redis) | **Go 大幅领先** | — |
| **工具集** | Bash/**PowerShell**★/Read/Write/Edit/Grep/Glob + Task + Schedule + Skill + Team | file/shell/web/json/multimodal + Task/Schedule/Subagent/Skill + Team + A2A | **Python 略领先** | Go 缺专用 PowerShell 工具（仅 cmd.exe 兜底） |
| **模型后端** | **9 家** + Ollama + **Moonshot/Kimi K3**★ | 10 家(含 vLLM) + Responses API + Router + 断路器 + ModelCard | **Go 略领先** | Python 新增 Moonshot 原生支持 |
| **嵌入模型** | 6 类(含 DashScope 多模态) | 5 类 + FileCache + ModelCard + ONNX 代理 | **基本持平** | — |
| **Workspace 沙箱 ★** | **8 个后端**：Local/Docker/E2B/**K8s**★/**Daytona**★/**Bubblewrap**★/**Apple Container**★/**OpenSandbox**★ + MCP Gateway + workspace_manager | **3 个后端**：Local/Docker/E2B + MCP Gateway + Offloader | **Python 大幅领先** | Go 缺 5 个沙箱后端。**本轮最大新增差距。** |
| **权限/HITL** | PermissionEngine + 5 模式 + **`on_check_permission` 中间件钩子**★ + **Agent 中断优雅处理**★ | PermissionEngine + Bash AST + 5 模式 | **Python 领先** | Go 缺权限中间件钩子、Agent 中断状态清理 |
| **中间件钩子数 ★** | **7 个**（on_reply/reasoning/**on_check_permission**★/on_acting/on_model_call/**on_compress_context**★/on_system_prompt） | **5 个**（Reply/Reasoning/Acting/ModelCall/SystemPrompt） | **Python 领先** | Go 缺权限检查 + 上下文压缩两个拦截点 |
| **运行时状态注入 ★** | **InjectionConfig**：自动注入当前时间（可配时区/间隔）、任务状态、上下文长度警告 + **extra_fields** 用户自定义字段 | `runcontext/` 包存在但**无自动注入** | **Python 领先** | Go 缺自动时间/任务/上下文注入 |
| **存储层** | `redis_storage`(64KB) + **SQLAlchemy 异步存储**★(Postgres/MySQL/SQLite + Alembic 迁移) + base(29KB) | `redis_storage`(20KB) + memory_storage(10KB) | **Python 领先（深度+广度）** | Go 缺 SQL 存储后端 |
| **可观测性/Tracing** | OTel + `middleware/_tracing` extractor(20KB)+trace(12KB)+attributes(7KB) | OTel + LangSmith + Langfuse + TracingMiddlewareAdapter + otelbridge | **基本持平** | — |
| **自演化/GEP** | Roadmap | evolver/(Gene/Capsule/Evolver 16 方法/Skill2GEP/MCP) | **Go 领先** | — |
| **Plugin 系统** | 无 | plugin/(接口/Manager/Registrar/YAML/.so) | **Go 领先** | — |
| **实时语音** | TTS(DashScope/Gemini/OpenAI) | AudioModel + TTS 独立包 + Voice 示例 + VAD + 打断 | **Go 领先** | — |
| **文档/社区/分发** | docs.agentscope.io + PyPI + ~50 贡献者 + 论文 | MkDocs 站点 + go get + 3 贡献者 | **Python 大幅领先** | 见旧版方案，本版不重复 |

---

## 2. Python 领先点深度剖析（按严重度分级，2026-07-30 更新）

> Phase 5-8 已补齐上一轮 TIER 1-3 的大部分差距（RAG KB 管道、CoordBus、Web UI、Agentic Memory、Tracing、Task CRUD、权限模式）。以下聚焦 **仍然存在的差距** 和 **最近 5 周新增的差距**。

### 🔴 TIER 1：关键能力差距（直接影响生产可用性与开发者采用）

#### 2.1 ★ Workspace 沙箱多元化（本轮最大新增差距）

**Python 现状**（8 个后端，最近 5 周新增 5 个）：

| 后端 | 代码量 | 新增时间 | 说明 |
|------|--------|----------|------|
| Local | 30KB | 原有 | 本地文件系统 |
| Docker | 27KB | 原有 | 容器隔离 + Dockerfile 生成 |
| E2B | 22KB | 原有 | 云沙箱 |
| **K8s** ★ | 38KB | 近 5 周 | Pod/PVC 生命周期 + tar-stream + MCP Gateway |
| **Daytona** ★ | 28KB | 近 5 周 | Daytona 开发环境 |
| **Bubblewrap** ★ | 47KB | 近 5 周 | Linux 用户命名空间沙箱 |
| **Apple Container** ★ | 28KB | 近 5 周 | macOS Containerization 框架 |
| **OpenSandbox** ★ | 28KB | 近 5 周 | OpenSandbox 远程沙箱 |

**Go 现状**：仅 3 个后端（Local/Docker/E2B），与 Python 原有对齐，但新增 5 个全部缺失。
**差距结论**：Python 的沙箱生态覆盖了 Linux/macOS/K8s/云开发环境/轻量隔离等多种场景，Go 版在"多环境部署"上严重滞后。

#### 2.2 ★ SQL 存储后端（新增差距）

**Python 现状**：`app/storage/_sql/` —— SQLAlchemy 2.0 异步存储：
- 支持 SQLite(`aiosqlite`)、PostgreSQL(`asyncpg`)、MySQL/MariaDB
- 表结构声明式定义 + Alembic 迁移脚本
- 原子 upsert（方言原生 `ON CONFLICT`/`ON DUPLICATE KEY`）
- 级联删除（agent → sessions → messages → teams）
- 知识库文档租约管理（acquire/renew/release CAS + 过期清扫）
- 与 Redis 存储并行，用户可选

**Go 现状**：仅 `RedisStorage`(20KB) + `MemoryStorage`(10KB)，无 SQL 后端。
**差距结论**：许多生产环境偏好 SQL 存储（ACID、查询能力、运维成熟度），Go 缺这一选项限制了部署灵活性。

#### 2.3 RAG 解析器与向量库扩展（Phase 5 遗留差距）

**Python 新增**（最近 5 周）：
- **WordParser**（341 行）：`.docx` 解析，段落/表格(Markdown/JSON)/嵌入图片(现代 `w:drawing` + 旧式 VML `w:pict`)，合并单元格处理
- **ExcelParser**（462 行）：`.xlsx`/`.xls` 解析，逐 sheet 表格渲染(Markdown/JSON)，嵌入图片提取
- **MongoDBStore**（464 行）：`$vectorSearch` 聚合、Atlas + 自托管、索引就绪轮询
- **ElasticsearchStore**（297 行）：`dense_vector` 索引、cosine kNN、SHA-256 确定性 ID

**Go 现状**：
- 解析器：4 个（Text/PDF/PPTX/Image），缺 Word/Excel
- 向量库：ES 和 PgVector 仍为占位（`ErrNotImplemented`），无 MongoDB

---

### 🟠 TIER 2：重要能力差距（影响框架精细度与可扩展性）

#### 2.4 ★ 中间件钩子精细化（新增差距）

**Python 现状**（7 个钩子，最近 5 周新增 2 个）：
1. `on_reply` — 洋葱模型
2. `on_reasoning` — 洋葱模型
3. **`on_check_permission`** ★ — 洋葱模型，拦截权限检查，可替换/绕过决策
4. `on_acting` — 洋葱模型
5. `on_model_call` — 洋葱模型
6. **`on_compress_context`** ★ — 洋葱模型，拦截上下文压缩
7. `on_system_prompt` — 管道模型

**Go 现状**：5 个钩子（Reply/Reasoning/Acting/ModelCall/SystemPrompt），缺权限检查和上下文压缩拦截点。
**差距结论**：`on_check_permission` 允许中间件在权限决策链中插入自定义逻辑（如审计、动态策略）；`on_compress_context` 允许控制压缩行为。这些是框架可扩展性的关键。

#### 2.5 ★ 运行时状态注入（新增差距）

**Python 现状**：`InjectionConfig` 自动注入：
- **当前时间**：可配时区、格式、注入间隔（默认 0.5h），`<system-reminder>` 标签包裹
- **任务状态**：检测 Task 工具调用，避免重复注入
- **上下文长度警告**：`context_buffer_ratio` 触发压缩前预警
- **extra_fields**：用户自定义键值对注入
- 以 `HintBlock` 形式注入对话上下文

**Go 现状**：`runcontext/` 包存在但无自动注入机制，需手动通过 `SystemPromptTransformer` 中间件实现。
**差距结论**：Agent 的时间感知、任务追踪、上下文管理是生产级 Agent 的基础体验。

#### 2.6 ★ PowerShell 专用工具（新增差距）

**Python 现状**：`_powershell.py`（281 行）—— 专用 PowerShell 工具：
- 自动探测 `pwsh`（优先）或 `powershell.exe`
- Base64 UTF-16-LE 编码命令（`-EncodedCommand`）
- `-NoLogo -NoProfile -NonInteractive` 标志
- 输出上限 30,000 字符 + 超时上限 600s
- 所有命令强制 ASK 权限确认

**Go 现状**：`tool/shell/` 在 Windows 上使用 `cmd.exe /c`，无专用 PowerShell 工具、无 PS 验证器、无 PS AST 解析。
**差距结论**：Windows 环境下 PowerShell是主力工具，Go 版对 Windows 开发者体验不足。

#### 2.7 ★ 资源跨用户共享策略（新增差距）

**Python 现状**：`app/access/_policy.py`（163 行）：
- `ResourceKind`：CREDENTIAL/AGENT/KNOWLEDGE_BASE
- `ResourcePermission`：READ/EDIT
- `ResourceAccessPolicyBase`：可扩展的跨所有者资源访问决策
- `DenyAllResourceAccessPolicy`：默认拒绝所有跨用户访问

**Go 现状**：RBAC 有 `OrgID` 组织隔离，但无显式的跨用户资源共享策略框架。
**差距结论**：多租户场景下，用户间需要可控地共享 Agent/KB/Credential，Go 缺此扩展点。

#### 2.8 存储层深度（持续差距）

**Python 现状**：`redis_storage`(64KB) + SQL 存储(1428+ 行) + base(930 行) —— 全实体 CRUD + 投影 + 索引态 + 调度态 + 租约管理。
**Go 现状**：`redis_storage`(20KB) + memory_storage(10KB)。
**差距结论**：Go 实体覆盖到位但深度不足（投影、索引态、复杂查询），且缺 SQL 后端。

---

### 🟡 TIER 3：中度差距（体验与完整度）

#### 2.9 ★ Agent 中断优雅处理（新增差距）
Python `UserInterruptEvent`：中断时关闭未完成的工具调用、清理状态、标记 ToolCallBlock 为 INTERRUPTED。Go 有中断机制（原子标志 + channel）但缺工具调用状态清理。

#### 2.10 ★ 用户自定义压缩 hints（新增差距）
Python 允许用户在压缩时保留特定内容（`user_defined_hints`），Go 的上下文压缩无此机制。

#### 2.11 ★ 上下文压缩结构化 schema（新增差距）
Python `SummarySchema` Pydantic 模型：task_overview/current_state/important_discoveries/next_steps/context_to_preserve 五段式结构化摘要。Go 有 LLM 元压缩但 schema 不如 Python 精细。

#### 2.12 SubAgentTemplate 声明式模板
Python `SubAgentTemplate`：声明式定义子 agent 类型(type/system_prompt_template/permission_context)。Go 有 `SubagentTemplates` 但声明式成熟度略低。

#### 2.13 ★ 流式性能优化（新增差距）
Python 修复了 O(n²) → O(n) 的流式累积性能问题。Go 需审视是否有类似问题。

---

## 3. Go 版差异化优势（须继续放大，勿在补齐中稀释）

| 优势 | 现状 | 放大方向 |
|------|------|----------|
| **A2A 分布式协议** | ShardRouter/ClusterManager/Redis 注册/安全/故障转移 watch | 打造成"多语言 Agent 网格"事实标准 |
| **ReMe 记忆** | 文件/向量/Hybrid/Dream/KG/SQLiteVec + AgenticMemory | "被动+主动+自主"三模记忆体系 |
| **GEP 自演化** | evolver/ Gene/Capsule/Skill2GEP/MCP | 闭环落地为"自愈 Agent"卖点 |
| **Plugin 系统** | .so 动态加载 + YAML 配置 | 生态插件市场雏形 |
| **ONNX 推理代理** | CLIP/Whisper 预处理 + HTTP 代理 | 本地多模态零 CGO |
| **性能工程** | pprof + benchmark Catalog + Makefile 对比 | 发布权威性能报告 |
| **断路器/DAG/Streamable HTTP** | 三状态断路器 + Kahn DAG + MCP 流式 HTTP | 生产稳定性卖点 |
| **TTS 独立包** | DashScope/OpenAI + RealtimeModel | 实时语音领先 |
| **向量库广度** ★ | 6 个完整后端(Local/SQLiteVec/Chroma/Milvus/Qdrant/Weaviate) | 比 Python(4 个)多 SQLiteVec/Chroma/Weaviate，保持领先 |
| **CoordBus 协调原语** ★ | Lock/Registry/Queue/Log Local+Redis | 与 Python 持平，继续深化跨会话协调 |

---

## 4. 演进总体战略

> **愿景**：让 AgentScope.Go 成为"**云原生、高并发、长记忆、自演化、托管式** Agent 服务的首选 Go 框架"。
> 补齐"托管服务化"能力带（RAG KB / 消息总线 / 服务编排 / 现代 UI），同时守住分布式与前沿特性领先。

### 4.1 战略原则

1. **补齐不复制**：每个 Python 能力用 Go 地道方式实现——单二进制、`embed.FS` 内嵌资源、interface 组合、channel 背压，拒绝逐行翻译。
2. **能力带优先**：本轮聚焦 TIER 1（RAG KB / 消息总线 / UI），它们是"生产可用"的门槛，不是锦上添花。
3. **守住护城河**：A2A/ReMe/evolver/Plugin 是 Go 独有，每个新阶段都要有"放大既有优势"的任务，禁止只补不攻。
4. **最小依赖**：parser 优先纯 Go 库（`unioffice`/PPT、`pdf`/PDF、`golang.org/x/image`），blob 首版只做 Local+S3，避免一上来铺全家桶。
5. **可验证**：每阶段交付 = 可运行示例 + 通过的测试（`go test ./... -race`）+ 文档。

### 4.2 演进路线总图（v3 修订）

```
2026-Q3  Phase 5-8 ✅ 完成：RAG KB / CoordBus / Web UI / Agentic Memory / Tracing
    │
    │   Python 最近 5 周新增差距：5 个 Workspace 后端 / SQL 存储 / 2 个中间件钩子
    │   / 运行时注入 / Word+Excel 解析器 / MongoDB+ES 向量库 / PowerShell 工具
    │
    ▼
Phase 9（本轮 P0）：Workspace 沙箱扩展  ← 最大新增差距
    │   K8s / Bubblewrap / Daytona / OpenSandbox / Apple Container
    ▼
Phase 10（本轮 P1）：存储深化 + 中间件精细化
    │   SQL 存储后端 / on_check_permission + on_compress_context 钩子
    │   / 运行时状态注入 / PowerShell 工具 / 资源跨用户共享
    ▼
Phase 11（本轮 P2）：RAG 扩展 + 向量库补全
    │   Word/Excel 解析器 / MongoDB+ES 向量库实现 / S3 BlobStore
    │
    （旧 Phase 1-8 中已完成项不再重复；未完成项见 TODO.md）
```

---

## 5. 分阶段演进方案（本轮新增）

### Phase 5：RAG 托管知识库服务（P0，目标 2026-Q3）

> 对标 Python `rag/` + `app/rag/` + `middleware/_rag.py`。用 Go 纯库实现解析/分块，避免重依赖。

#### 5.1 文档模型与解析器 `rag/document/` + `rag/parser/`
- `document.go`：`Document`/`Section`/`Chunk` 数据模型（对齐 Python，含 metadata slide_index/media_type），**替换现有 155 字节桩**。
- `parser/parser.go`：`Parser` 接口 `Parse(ctx, reader, mediaType) ([]Section, error)` + `SupportedMediaTypes()` + 注册表 `Registry`（按 mediaType 路由）。
- `parser/text.go`：TextParser（纯 stdlib）。
- `parser/pdf.go`：PDFParser，基于 `github.com/ledongthuc/pdf` 或 `github.com/dslipak/pdf`（纯 Go，无 CGO）。
- `parser/pptx.go`：PPTXParser，基于 `github.com/unidoc/unioffice` 或 `github.com/pterm/ptunioffice`（逐页 + 表格 + 图片提取）。
- `parser/image.go`：ImageParser（base64 + media_type，OCR 留接口，首版可调外部 OCR/多模态模型）。

#### 5.2 分块器 `rag/chunker/`
- `chunker.go`：`Chunker` 接口 `Chunk([]Section) ([]Chunk, error)`。
- `approx_token.go`：`ApproxTokenChunker`——`len(utf8)/4` 近似 token，chunk_size/overlap 可配，DataBlock 原样透传（对齐 Python，零 tokenizer 依赖）。
- 预留 `recursive.go` 递归分块（Phase 后续）。

#### 5.3 Blob 存储 `rag/blob/`
- `blob.go`：`BlobStore` 接口 `Put(ctx, key, reader) (uri, error)` / `Get(ctx, uri) (io.ReadCloser, error)` / `Delete`。
- `local.go`：`LocalBlobStore`（基于 `os`，根目录可配）。
- `s3.go`：`S3BlobStore`（基于 `github.com/aws/aws-sdk-go-v2`，Phase 5.3 可选，首版 Local 即可验收）。

#### 5.4 知识库管理器 `rag/kb/`
- `kb.go`：`KnowledgeBase` 运行时句柄（绑定 embedding+vectorstore+collection，懒建表，search/insert/delete/list，`metadata_filter` 多租户隔离）。
- `manager.go`：`KBManager` 接口（生命周期 Create/Get/List/Delete + 运行时句柄获取）+ `CollectionPerKB` 实现（一 KB 一 collection，复用现有 `memory/vector` 后端）。
- `dimension.go`：`DimensionPolicy`（embedding 维度一致性校验）。

#### 5.5 索引工作流 `rag/index/`
- `worker.go`：`IndexWorker`——消费索引任务 → 读 blob → parse → chunk → embed → 写 vectorstore，含心跳(`context`)+错误标记。
- `consumer.go`：`IndexTaskConsumer`——从消息总线队列消费任务（依赖 Phase 6 的 queue 原语；Phase 5 可先用 channel 内部队列）。
- `sweeper.go`：`IndexSweeper`——扫描失败任务重试。
- **ponytail**：首版 worker 可单进程内 goroutine + channel，验证管道通后再接消息总线。

#### 5.6 RAG 中间件 `middleware/rag.go`
- `RAGMiddleware`：实现洋葱中间件——
  - `list_tools`：自动注入 `search_knowledge` 工具（按 KB 描述生成 schema）
  - `on_reasoning`：对用户输入跨 KB 检索，结果格式化为 `HintBlock` 注入上下文
  - `on_reply`：可选，记录检索命中
- 复用现有 `message.HintBlock` + `tool.Response` 规范。

#### 5.7 KB HTTP 路由 `gateway/kb_handlers.go`
- `GET/POST /api/v1/knowledge-bases`（CRUD）
- `POST /api/v1/knowledge-bases/{id}/documents`（上传文档 → 入 blob → 派发索引任务）
- `GET /api/v1/knowledge-bases/{id}/documents`（文档列表 + 索引状态）
- `DELETE /api/v1/knowledge-bases/{id}/documents/{doc_id}`
- `POST /api/v1/knowledge-bases/{id}/search`（手动检索）
- `gateway/app.go` 增加 `WithKBManager()` 链式选项。

#### 5.8 示例与测试
- `examples/rag_kb/`：上传 PDF/PPT → 索引 → 提问，端到端 demo。
- `rag/parser/*_test.go`：各 parser 单测（用 `testdata/` 固定样本）。
- `rag/chunker/*_test.go`：token 近似 + overlap + DataBlock 透传。
- `middleware/rag_test.go`：中间件注入与检索注入。
- **ponytail 检查**：`rag/parser/demo()` 或最小 `test_*` 确保管道断言可运行。

**Phase 5 验收**：
- [ ] PDF/PPT/Text/Image 4 parser 可解析真实样本
- [ ] ApproxTokenChunker 切分 + overlap 正确
- [ ] LocalBlobStore + KBManager(CollectionPerKB) CRUD 通过
- [ ] IndexWorker 单进程端到端：上传→索引→可检索
- [ ] RAGMiddleware 注入 search_knowledge 工具 + on_reasoning 检索
- [ ] KB HTTP 路由 CRUD + 上传 + 搜索通过
- [ ] `go test ./... -race` 全绿

---

### Phase 6：消息总线深化 + 服务编排层（P1，目标 2026-Q4）

#### 6.1 消息总线原语补齐 `messagebus/`
对齐 Python `MessageBus` 领域无关原语，在现有 `Bus` 接口上**追加**（不破坏 team_bus）：
- `Lock(ctx, key, ttl) (release func, error)`：分布式锁（Redis `SET NX EX` / Local `sync.Mutex` map）。
- `RegistrySet/Get/List/Delete(ctx, ns, key, val)`：注册表（复用 a2a 已有 Registry 思路，但泛化到 bus 层）。
- `QueuePush/QueuePop(ctx, name, val)`：通用队列（Redis `LPUSH/BRPOP` / Local channel）。
- `LogAppend/LogRead(ctx, ns, val, cursor)`：追加日志。
- `messagebus/keys.go`：业务键约定（wakeup kind: wake/resume、projection namespace）。
- **ponytail**：Local 后端用现成 stdlib；Redis 后端复用 `go-redis`，不引新依赖。

#### 6.2 跨会话投影 `gateway/projection.go`
- `SessionProjection`：一个会话可向另一会话投影 UI 卡片（如 worker 的 HITL 请求投影到 leader），存 bus registry/log。

#### 6.3 服务编排层 `gateway/app.go` 增强
- `CreateApp(opts...)` 工厂函数（对齐 `create_app`）：可插拔 Storage/Bus/WorkspaceManager/KBManager/BlobStore/ExtraMiddlewares/ExtraAgentTools/SubagentTemplates/CustomAgentClass。
- `ChatService` 编排：统一 `Run(ctx, sessionID, input)` → 事件投影 → 唤醒/卸载/取消分发（复用现有 wakeup_dispatcher + background_task + team_tools，提炼为编排层）。
- `gateway/middleware/`：新增领域中间件目录
  - `inbox_middleware.go`：团队消息排空注入（对齐 Python InboxMiddleware）
  - `state_change_middleware.go`：会话状态变更广播
  - `tool_offload_middleware.go`：超时工具卸载（已有 `gateway/tool_offload.go`，提炼为标准中间件接入编排）

**Phase 6 验收**：
- [ ] Bus 分布式锁/registry/queue/log 四原语 Local+Redis 双后端测试通过
- [ ] CreateApp 工厂可嵌入（作为子路由挂载）
- [ ] ChatService 统一编排：卸载→唤醒→团队消息 三路打通
- [ ] InboxMiddleware 排空 + 注入验证
- [ ] `go test ./... -race` 全绿

---

### Phase 7：现代 Web UI（P2，目标 2027-Q1）

#### 7.1 双轨策略（与旧版一致，深化执行）
**方案 A（主，Go 原生）**：embed SPA
- 用 Vite + React/Vue/Svelte 构建 `examples/web_ui/frontend/`，`go:embed dist` 内嵌进单二进制。
- 页面对齐 Python：Agent/会话/凭证/模型/知识库/权限/日程/团队/MCP 管理 + SSE 流式聊天。
- 复用现有 AG-UI 协议，前端直连 gateway。

**方案 B（兼容）**：验证 Python `examples/web_ui` React 前端可直连 Go gateway（AG-UI 协议兼容），文档给出"Python 前端 + Go 后端"部署方案。

#### 7.2 Studio 深化
- 现有服务端 Studio 增加：**知识库管理页**（对应 Phase 5 KB 路由）、**索引状态监控**、**Blob 浏览**。

**Phase 7 验收**：
- [ ] embed SPA 单二进制可启动，核心页面可用
- [ ] SSE 流式聊天端到端
- [ ] Python Studio 前端连 Go gateway 兼容性验证通过
- [ ] 文档：双轨部署方案

---

### Phase 8：记忆范式 / 可观测 / 存储 / 工具补全（P3，目标 2027-Q1）

#### 8.1 Agentic Memory `middleware/agentic_memory.go`
- 对齐 Python `_agentic_memory`：agent 自主决策记忆的存取。实现 `AgenticMemory` 接口（基于现有 ReMe 向量库），中间件暴露 `remember`/`recall`/`forget` 工具给 agent 自主调用，区别于被动检索的 ReMe。
- 与现有 `LongTermMemoryMiddleware`(static_control/agent_control/both) 区分：Agentic 强调"agent 自己决定记什么"。

#### 8.2 Tracing 提取深化 `observability/extractor.go`
- 对齐 Python extractor：从 Agent 事件提取丰富 span 属性（model/tool/usage/token/reasoning step），丰富 otelbridge。

#### 8.3 存储投影与索引态 `service/`
- `redis_storage.go` 增加：知识库索引态、session projection、调度态的读写。

#### 8.4 Task 工具 CRUD 补全 `tool/task/`
- 对齐 Python：TaskUpdate 支持状态机（todo/in_progress/done/blocked）+ 依赖 + 依赖结果传递。

#### 8.5 预设权限模式 `permission/`
- 新增 `PermissionMode` 常量：`EXPLORE`(只读)/`STANDARD`/`AUTONOMOUS` 等，可直接用于 SubAgentTemplate。

**Phase 8 验收**：
- [ ] Agentic Memory 中间件：agent 自主 remember/recall 验证
- [ ] Tracing span 属性粒度对齐 Python
- [ ] TaskUpdate 状态机 + 依赖通过
- [ ] 预设权限模式可一键应用
- [ ] `go test ./... -race` 全绿

---

## 5B. 分阶段演进方案（v3 新增，Phase 9-11）

### Phase 9：Workspace 沙箱扩展（P0，目标 2026-Q4）

> 对标 Python 最近 5 周新增的 5 个 Workspace 后端。Go 现有 3 个(Local/Docker/E2B)，需扩到 8 个。

#### 9.1 Kubernetes Workspace `workspace/k8s/`
- `k8s_workspace.go`：Pod 生命周期管理（create/exec/delete），PVC 挂载，tar-stream 文件传输
- `k8s_backend.go`：K8s API 封装（`client-go`），Pod spec 模板，资源限制
- `constants.go`：默认镜像/标签/命名空间
- MCP Gateway 集成：Pod 内 MCP server 通过 workspace gateway 暴露
- 复用现有 `workspace.Workspace` 接口（ReadFile/WriteFile/ListDir/Execute/Close）

#### 9.2 Bubblewrap Workspace `workspace/bubblewrap/`
- `bubblewrap_workspace.go`：Linux 用户命名空间沙箱（`bwrap` 命令），零 root 权限
- `bubblewrap_backend.go`：挂载点管理（只读/读写分离），网络隔离选项
- 适用场景：Linux 生产环境轻量隔离，比 Docker 更轻量

#### 9.3 Daytona Workspace `workspace/daytona/`
- `daytona_workspace.go`：Daytona REST API 客户端，开发环境创建/销毁
- `daytona_backend.go`：文件操作 + 命令执行 via Daytona API
- 适用场景：云开发环境

#### 9.4 OpenSandbox Workspace `workspace/opensandbox/`
- `opensandbox_workspace.go`：OpenSandbox REST API 客户端
- `opensandbox_backend.go`：沙箱生命周期 + 文件/命令操作
- 适用场景：远程沙箱执行

#### 9.5 Apple Container Workspace `workspace/applecontainer/`
- `applecontainer_workspace.go`：macOS Containerization framework 适配
- `applecontainer_backend.go`：容器创建/文件/命令
- 适用场景：macOS 原生隔离（build tag `darwin`）

#### 9.6 示例与测试
- `examples/workspace_k8s/`：K8s workspace 端到端 demo
- 各后端集成测试（`//go:build integration`）
- 统一 Workspace 接口一致性测试

**Phase 9 验收**：
- [ ] K8s workspace：Pod 创建/文件传输/命令执行/销毁通过
- [ ] Bubblewrap workspace：Linux 沙箱隔离验证
- [ ] Daytona/OpenSandbox workspace：API 调用 + 文件/命令通过
- [ ] Apple Container：macOS 编译通过 + 基本功能验证
- [ ] `go test ./... -race` 全绿

---

### Phase 10：存储深化 + 中间件精细化（P1，目标 2027-Q1）

#### 10.1 SQL 存储后端 `service/sql_storage/`
- `storage.go`：实现 `Storage` 接口，基于 `database/sql` + `sqlx`
- `tables.go`：表结构声明（users/sessions/agents/credentials/messages/teams/knowledge_bases/knowledge_documents）
- `migrations.go`：Schema 迁移（内嵌 SQL，Go migrate 库或自实现）
- 支持 SQLite（`modernc.org/sqlite` 纯 Go）+ PostgreSQL（`pgx`）+ MySQL（`go-sql-driver`）
- 原子 upsert（方言适配）+ 级联删除 + 事务
- `gateway/app.go` 增加 `WithSQLStorage(dsn)` 链式选项

#### 10.2 中间件钩子扩展 `middleware/`
- `middleware.go` 新增两个拦截点：
  - `PermissionInterceptor`：`OnCheckPermission(ctx, agent, toolCall, tool, input, next) (Decision, error)` —— 拦截权限检查
  - `CompressionInterceptor`：`OnCompressContext(ctx, agent, messages, next) (*Msg, error)` —— 拦截上下文压缩
- `chain.go` 更新：自动分类新钩子类型，构建对应拦截链
- 向后兼容：现有 5 钩子不变，新钩子为可选接口

#### 10.3 运行时状态注入 `runcontext/injection.go`
- `InjectionConfig`：可配时区/时间格式/注入间隔/context_buffer_ratio/extra_fields
- `InjectionMiddleware`：实现 `SystemPromptTransformer` + `OnReasoning`
  - 自动注入当前时间（`<system-reminder>` 标签，间隔触发）
  - 检测 Task 工具调用避免重复注入
  - 上下文长度预警（接近 trigger_ratio 时注入 HintBlock）
  - extra_fields 用户自定义键值对
- 复用现有 `message.HintBlock`

#### 10.4 PowerShell 专用工具 `tool/shell/powershell.go`
- `PowerShellTool`：自动探测 `pwsh`（优先）或 `powershell.exe`
- Base64 UTF-16-LE 编码命令（`-EncodedCommand`）
- `-NoLogo -NoProfile -NonInteractive` 标志
- 输出上限 30,000 字符 + 超时上限 600s
- 权限模型：所有命令强制 ASK
- 复用现有 `tool.Tool` 接口 + `permission.Engine`

#### 10.5 资源跨用户共享 `service/access/`
- `policy.go`：`ResourceKind`(Credential/Agent/KB) + `ResourcePermission`(READ/EDIT) + `ResourceAccessPolicy` 接口
- `DenyAllPolicy`：默认拒绝跨用户访问
- `gateway/app.go` 增加 `WithResourceAccessPolicy(policy)` 链式选项

**Phase 10 验收**：
- [ ] SQL 存储后端：SQLite + PostgreSQL 双方言 CRUD 测试通过
- [ ] `on_check_permission` 钩子：中间件可拦截/替换权限决策
- [ ] `on_compress_context` 钩子：中间件可控制压缩行为
- [ ] 运行时注入：时间/任务/上下文预警自动注入验证
- [ ] PowerShell 工具：Windows 环境 pwsh 命令执行通过
- [ ] 资源共享策略：DenyAll 默认行为 + 可扩展接口
- [ ] `go test ./... -race` 全绿

---

### Phase 11：RAG 扩展 + 向量库补全（P2，目标 2027-Q1）

#### 11.1 Word 解析器 `rag/parser/word.go`
- `WordParser`：`.docx` 解析，基于纯 Go `archive/zip` + `encoding/xml`
- 段落提取（`<w:p>` + `<w:r>` + `<w:t>`）
- 表格提取（`<w:tbl>` + `<w:tr>` + `<w:tc>`），Markdown 渲染
- 嵌入图片提取（`<w:drawing>` + VML `<w:pict>`）
- 合并单元格处理（`gridSpan`/`vMerge`）

#### 11.2 Excel 解析器 `rag/parser/excel.go`
- `ExcelParser`：`.xlsx` 解析，基于纯 Go `archive/zip` + `encoding/xml`
- 逐 sheet 表格提取，Markdown pipe-table 渲染
- 嵌入图片提取
- 可选 sheet 名称/单元格坐标注入

#### 11.3 Elasticsearch 向量库实现 `memory/vector/elasticsearch_store.go`
- 替换现有占位，实现完整 `VectorStore` 接口
- 基于 `olivere/elastic` 或 `elastic/go-elasticsearch`
- `dense_vector` 索引映射 + cosine kNN 搜索
- SHA-256 确定性 ID + metadata filter

#### 11.4 MongoDB 向量库 `memory/vector/mongodb_store.go`
- 新增 MongoDB 后端，`$vectorSearch` 聚合
- Atlas + 自托管支持
- 索引就绪轮询

#### 11.5 S3 BlobStore `rag/blob/s3.go`
- 基于 `aws/aws-sdk-go-v2`
- 实现 `BlobStore` 接口（Put/Get/Delete）

**Phase 11 验收**：
- [ ] Word 解析器：真实 .docx 样本解析通过
- [ ] Excel 解析器：真实 .xlsx 样本解析通过
- [ ] Elasticsearch 向量库：CRUD + kNN 搜索通过
- [ ] MongoDB 向量库：CRUD + vectorSearch 通过
- [ ] S3 BlobStore：上传/下载/删除通过
- [ ] `go test ./... -race` 全绿

---

## 6. 跨阶段工程化与社区（贯穿，承接旧版）

> 旧版 Phase 1-4 的 CI/文档/示例/发布任务多数已完成（见 TODO.md）。本轮聚焦：

- **测试**：每个新增 parser/chunker/blob/kb/index 模块必须带 `test_*`；KB 路由加集成测试（`//go:build integration`）。
- **文档**：`docs/RAG.md`（托管 KB 教程）、`docs/MESSAGE_BUS.md`（总线原语）、`docs/WEB_UI.md`（双轨部署）。
- **示例**：`examples/rag_kb/`、`examples/agentic_memory/`。
- **性能**：KB 索引吞吐 + 检索延迟纳入 `benchmark/` Catalog。
- **契约**：保持 RAG document/chunk JSON 与 Python 兼容（`tests/cross_lang`）。

---

## 7. 关键风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| K8s workspace 依赖 `client-go` 过重 | 中 | 中 | 使用 `k8s.io/client-go` 最小化依赖；可选 build tag 隔离 |
| Bubblewrap 仅 Linux | 低 | 低 | build tag `linux` 隔离；其他平台跳过 |
| Apple Container 仅 macOS 15+ | 低 | 低 | build tag `darwin` 隔离；CGO 可能需要 |
| SQL 方言差异导致 bug | 中 | 中 | 抽象方言层 + 双方言 CI 测试（SQLite + PostgreSQL） |
| Word/Excel 纯 Go 解析覆盖不全 | 中 | 中 | 复杂格式先留接口接多模态模型；保留 Tika 作 fallback |
| 新中间件钩子破坏现有链 | 低 | 高 | 新钩子为可选接口，不实现则跳过；向后兼容 |
| 与 Python 事件/文档格式分叉 | 低 | 高 | `tests/cross_lang` 契约测试覆盖 document/chunk/KB JSON |
| 工作量过大稀释领先项 | 中 | 高 | 每 Phase 强制含"放大既有优势"任务；P0 完成再开 P1 |

---

## 8. 验收指标（v3 本轮，Phase 9-11）

| 指标 | Phase 5-8 后现状 | v3 目标（Phase 9-11） | 当前状态 |
|------|-----------------|----------------------|----------|
| Workspace 后端数 | 3(Local/Docker/E2B) | **8**(+K8s/Bubblewrap/Daytona/OpenSandbox/Apple Container) | **7**(+K8s/Bubblewrap/Daytona/OpenSandbox)，Apple Container 待做 |
| SQL 存储后端 | 无 | SQLite + PostgreSQL | **SQLite** ✅ |
| 中间件钩子数 | 5 | **7**(+on_check_permission/on_compress_context) | **7** ✅ |
| 运行时状态注入 | 无 | 自动时间/任务/上下文注入 | **有** ✅ |
| PowerShell 工具 | 无 | 专用 pwsh 工具 | **有** ✅ |
| RAG parser 数 | 4(Text/PDF/PPTX/Image) | **6**(+Word/Excel) | **6** ✅ |
| 向量库完整实现 | 6(+2 占位) | **8**(ES+MongoDB 替换占位) | 6（ES/MongoDB 待做） |
| S3 BlobStore | 无 | 有 | 待做 |
| 资源跨用户共享 | 无 | DenyAll + 可扩展接口 | **有** ✅ |
| 测试覆盖率 | ~65% | 70%+ | ~68% |
| `go test ./... -race` | 全绿 | 全绿 | 全绿 ✅ |

---

## 9. 优先级与执行顺序建议

```
P0（立即）  Phase 9  Workspace 沙箱扩展    ← 最大新增差距（5 个后端）
P1（次优先）Phase 10 存储深化 + 中间件精细化 ← SQL 存储 + 钩子 + 注入 + PowerShell
P2         Phase 11 RAG 扩展 + 向量库补全   ← Word/Excel + MongoDB/ES

并行（始终）守住并放大 A2A/ReMe/evolver/Plugin/ONNX 领先项
```

> 建议从 **Phase 9.1-9.2（K8s + Bubblewrap workspace）** 起步——K8s 是生产环境最常见沙箱，Bubblewrap 是 Linux 轻量隔离的刚需。两者均为纯 Go 实现、无重大外部依赖。

---

## 10. 结语

v2 方案（Phase 5-8）成功补齐了"托管服务化"能力带（RAG KB / CoordBus / Web UI / Agentic Memory），Go 版 v2.4.0 已成为功能最完整的 Go 原生 Agent 框架之一。但 Python 版在最近 5 周（67 commits）再次迭代，在 **Workspace 沙箱多元化**（3→8 后端）、**存储后端广度**（+SQL）、**中间件精细化**（5→7 钩子）、**RAG 解析器扩展**（+Word/Excel）四个维度拉开了新差距。本方案（Phase 9-11）以 Go 地道方式（`client-go` K8s、`bwrap` 沙箱、`database/sql` 存储、纯 Go XML 解析）补齐新增差距，同时守住 A2A/ReMe/evolver/Plugin 等独有护城河。优先级明确：**Phase 9（Workspace 沙箱）是本轮 P0**，它是"Go 版能否覆盖多环境部署"的分水岭。
