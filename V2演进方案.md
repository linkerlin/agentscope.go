# AgentScope.Go V2 演进方案

> **版本**: 2026-06-07（第 5 版）  
> **基准对比**: AgentScope Python **v2.0.0**（tag `v2.0.1`，`src/agentscope` ~31,900 行）vs. AgentScope.Go（commit **`3bf5cb1`**，~29,450 行非测试 Go 代码）  
> **核心命题**: Python v2 已完成从"消息为中心"到"事件为中心"的范式转移，并配套 `app/` 服务层（AG-UI、Scheduler 工具、MCP Gateway、Tool Offload 等）。Go 版 P0/P1/P2 地基已夯实，且在 **Memory、A2A、SubagentTool、E2B Workspace** 等领域反超或追平。**本次第 5 版对比修正了 E2B 实现状态（已由 stub 升级为完整实现），并新识别 MCP Gateway、Bash AST 解析器、文件读缓存、Schedule Agent 工具、Formatter 多 Agent 变体等差距。**

---

## 目录

1. [执行摘要](#1-执行摘要)
2. [Python v2 范式转移解析](#2-python-v2-范式转移解析)
3. [Go 版现状评估](#3-go-版现状评估)
4. [差距分析矩阵](#4-差距分析矩阵)
5. [V2 演进总体设计](#5-v2-演进总体设计)
6. [第一阶段：事件驱动范式重构（P0）— 已完成](#6-第一阶段事件驱动范式重构p0--已完成)
7. [第二阶段：生产级能力补齐（P1）— 基本完成](#7-第二阶段生产级能力补齐p1--基本完成)
8. [第三阶段：服务化与生态扩展（P2）— 基本完成，存在新增差距](#8-第三阶段服务化与生态扩展p2--基本完成存在新增差距)
9. [关键落后项深度分析（更新）](#9-关键落后项深度分析更新)
10. [第四阶段：前瞻布局（P3）](#10-第四阶段前瞻布局p3)
11. [风险与缓解](#11-风险与缓解)
12. [演进时间线与里程碑](#12-演进时间线与里程碑)
13. [实施状态跟踪](#13-实施状态跟踪)
14. [附录](#14-附录)

---

## 1. 执行摘要

AgentScope Python v2（以下简称 PyV2）于 2026-05 正式发布，其本质并非功能堆砌，而是一次**架构范式转移**：从以 `Msg` 为核心的请求-响应模型，转向以 `AgentEvent` 为核心的流式状态机模型。

AgentScope.Go（以下简称 Go 版）在 2026-04-15 至 2026-06-07 期间完成了密集的 V2 演进迭代。截至当前基准（commit `3bf5cb1`），Go 版已完成：

- **P0 事件驱动范式**：`event/` 20+ 事件类型、`agent/react` 真事件流 `ReplyStream` + `AgentState` 状态机 + 挂起恢复、`gateway/` V2 SSE/WebSocket 端点
- **P1 生产级能力**：`workspace/` Local + Docker + **E2B（完整 FS/命令执行，envd Connect-RPC）**、`permission/` 规则引擎 + `safety.go` 启发式校验、`toolkit/` 洋葱中间件链 + MCP Server 适配、`service/` Storage 抽象 + Redis + AES-GCM 加密、`schedule/` Cron 调度器、`tool/subagent/` 递归子 Agent（**PyV2 无对等实现**）
- **P2 服务化雏形**：多租户认证（API Key + JWT）、管理端点 CRUD、A2A V2 适配器、OpenTelemetry 追踪、LangSmith 可观测性、Gateway Session State Persistence 闭环

**本次深度对比关键差距（含第 5 版新增/修正）**：

| 差距项 | Python v2 | Go 版 | 影响 |
|--------|-----------|-------|------|
| **AG-UI Protocol** | `app/_middleware/_protocol/_agui.py` (257 行) | ✅ `gateway/agui.go` | Studio UI 对接 |
| **Task 内置工具** | `tool/_task/` 4 个工具 (Create/Get/List/Update) | ✅ `tool/task/` | Agent 自管理任务生命周期 |
| **Schedule Agent 工具** | `app/_manager/_scheduler/_tools/` 4 个工具 | ✅ `tool/schedule/` | Agent 自管理 Cron 调度 |
| **SkillViewer 工具** | `tool/_builtin/_skill.py` | ✅ `skill/skillviewer.go` | Agent 运行时浏览 Skill |
| **MCP Gateway** | `workspace/_mcp_gateway/` + `GatewayClient` | ✅ `workspace/mcp_gateway.go` + `gateway_mcp_client.go` | 沙箱内 MCP HTTP 代理 |
| **Bash AST 解析器** | `tool/_builtin/_bash_parser.py` (~883 行, tree-sitter) | ✅ 启发式 + 可选 `-tags treesitter` | `go test -tags treesitter ./permission/...` 启用 CGO tree-sitter-bash |
| **文件读缓存** | `state/_state.py` ToolContext LRU | ✅ `tool/file/cache.go` | mtime 感知 Read 缓存 |
| **Offloader 协议** | `workspace/_offload_protocol.py` 正式接口 | ✅ `workspace/offloader.go` | 大上下文外存卸载 |
| **外部工具执行** | `tool/_adapters.py` `is_external_tool` 全流程 | ✅ `agent/react/reply_stream.go` | `RequireExternalExecutionEvent` 已接线 |
| **Tool Offload 中间件** | `app/_middleware/_tool_offload_middleware.py` | ✅ `gateway/tool_offload.go` + hint 注入 | 长耗时工具后台化 + ReAct 注入 |
| **ModelCard / YAML** | `model/_model_card.py` + 33 个 YAML | ✅ `model/model_card.go` + `/api/v1/models` | 声明式模型配置 + HTTP API |
| **Formatter 多 Agent** | 9 后端 × Chat + MultiAgent 变体 | ✅ `formatter/providers.go` | DeepSeek/Moonshot/xAI/Response 已补 |
| **Docker Workspace** | 1,229 行，含 Dockerfile 生成 | ✅ `workspace/docker.go` 增强 | Dockerfile 生成 + 健康检查 |
| **E2B Workspace** | 1,034 行 + MCP Gateway 集成 | ✅ FS/命令 + `e2b_mcp.go` sidecar | MCP Gateway 主机侧 bootstrap |
| **Web UI 示例** | `examples/web_ui/` 完整前后端 | ✅ `examples/web_ui/` | 轻量 HTML/JS + DashScope demo |

**Go 版领先项（PyV2 无对等实现）**：

| 领先项 | Go 版 | Python v2 |
|--------|-------|-----------|
| **A2A 协议** | `a2a/` ~826 行，V2Adapter 完整 | ❌ roadmap 未实现 |
| **SubagentTool** | `tool/subagent/` 递归调用 + 深度限制 | ❌ 无 subagent 抽象 |
| **ReMe Memory** | ~6,025 行，5 向量后端 + Hybrid Search | 无独立 memory 包（state 内嵌） |
| **vLLM 后端** | ✅ | ❌ |

**测试现状**：Go 174 测试文件 / 1,157 测试函数，全量 `go test ./...` 通过；PyV2 54 测试文件 / ~619 测试函数。

Go 版在 **Memory（ReMe 长期记忆）、并发性能、类型安全、A2A 协议、服务化架构** 等领域保持领先，但上述新增差距表明 P2 收尾阶段仍需聚焦补齐。

本方案更新后的核心目标：

> **在 P0/P1/P2 地基已经夯实的条件下，聚焦补齐 AG-UI 协议、Task/Schedule 工具、SkillViewer、MCP Gateway、Offloader 等差距，完善开发者文档，最终实现与 PyV2 的完全跨语言互操作能力。Go 版继续放大 Memory、A2A、SubagentTool 差异化优势。**

---

## 2. Python v2 范式转移解析

PyV2 的 rewrite 不是增量升级，而是以下四个维度的根本性重构：

### 2.1 通信原语：Msg → Event

| 维度 | v0.x / Go 版旧态 | PyV2 |
|------|------------------|------|
| **核心原语** | `Msg` (消息) | `AgentEvent` (事件) |
| **Agent 输出** | `reply() -> Msg` | `reply_stream() -> AsyncGenerator[AgentEvent, None]` |
| **粒度** | 整条消息 | 块级增量 (`TextBlockDelta`, `ToolCallDelta`, `ThinkingBlockDelta`...) |
| **生命周期** | 无显式状态 | `PENDING → ASKING → ALLOWED → SUBMITTED → FINISHED` |

PyV2 定义了 **20+ 事件类型**，覆盖 Agent 运行的每一个原子操作：

```
ReplyStartEvent
├── TextBlockStartEvent → TextBlockDeltaEvent* → TextBlockEndEvent
├── ThinkingBlockStartEvent → ThinkingBlockDeltaEvent* → ThinkingBlockEndEvent
├── ToolCallStartEvent → ToolCallDeltaEvent* → ToolCallEndEvent
├── RequireUserConfirmEvent  ← HITL 挂起点
├── RequireExternalExecutionEvent  ← 外部执行挂起点
├── ToolResultStartEvent → ToolResultTextDeltaEvent* → ToolResultEndEvent
└── ReplyEndEvent
```

**关键洞察**：PyV2 的 UI、A2A、Observability 全部建立在这一事件流之上。没有细粒度事件，就无法实现真正的实时工具渲染、人机确认、跨 Agent 协作。

### 2.2 Agent 运行时：无状态 → 状态机

PyV2 的 `Agent` 是一个**可挂起、可恢复的协程状态机**：

- `AgentState` 持久化 `reply_id`, `cur_iter`, `cur_summary`, `context`, `tool_context`, `permission_context`
- `load_state()` / `save_state()` 支持跨 HTTP 请求恢复对话
- `RequireUserConfirmEvent` 使 Agent 在 `SUBMITTED` 与 `FINISHED` 之间显式挂起，等待外部事件驱动恢复

这意味着 PyV2 的 Agent 服务可以是无状态的 FastAPI worker，Agent 状态存储在 Redis 中。

### 2.3 工具执行：函数调用 → 中间件化沙箱

PyV2 引入了 `Workspace` 抽象和 `PermissionEngine`：

- **Workspace**: 隔离工具执行环境（Local / Docker / E2B）
- **PermissionEngine**: 规则引擎，支持 glob 路径匹配、命令子串匹配，决策 `ALLOW / DENY / ASK / PASSTHROUGH`
- **Agent Middleware** (`middleware/`): 洋葱模式拦截 `on_reply / on_reasoning / on_acting / on_model_call` + 流水线 `on_system_prompt`
- **Toolkit Middleware** (`toolkit/middleware.go`): 仅包裹工具执行 `ExecuteTool`（Logging / Metrics / Tracing / Offload）
- **经典 Hook** (`hook/`): `pre_reply / before_model / after_model / post_reply / on_error` 等，与 Middleware 并存

工具不再是简单的函数调用，而是在**受控沙箱中经过权限校验、中间件处理、状态流转**的完整工作流。

### 2.4 部署形态：库 → 多租户服务

PyV2 的 `app/` 模块提供了完整的 FastAPI 服务：

- 多租户（用户隔离）
- 多会话（Session 管理）
- 多模型（Credential 管理）
- 定时任务（Scheduler / Cron）
- 存储抽象（`StorageBase` + `RedisStorage`）
- **AG-UI Protocol**: 原生支持 `ag_ui` 前端协议中间件
- **Background Task Manager**: 完整的后台任务生命周期管理

这使得 AgentScope 从"开发框架"升级为"部署平台"。

---

## 3. Go 版现状评估

### 3.1 已完成对齐领域（截至 2026-06-07）

| 领域 | 状态 | 说明 |
|------|------|------|
| **事件系统 `event/`** | ✅ 已完成 | 20+ 事件类型 + `Bus` + `MetricsHandler` HTTP 端点 + Block 生命周期 |
| **真事件流 `ReplyStream`** | ✅ 已完成 | `agent/react` 实现真流式，支持挂起/恢复，不 fallback 到 `Call` |
| **AgentState 状态机** | ✅ 已完成 | `reply_id`, `cur_iter`, 可序列化、挂起-恢复协议、HITL 注入 |
| **Formatter 层** | ✅ 已完成 | `Formatter` / `TruncatedFormatter` / `ThinkingFormatter` interface，5 后端实现 |
| **AgentBase 基类** | ✅ 已完成 | `agent.Base` struct embedding，统一生命周期 |
| **模型后端** | ✅ 已扩展 | 10 个后端（OpenAI, Anthropic, Gemini, DashScope, Ollama, DeepSeek, vLLM, Moonshot, xAI, OpenAI Response）|
| **ToolResponse** | ✅ 已规范 | `tool.Response` 替换裸 `any`，支持 `[]ContentBlock` |
| **Workspace** | ⚠️ 大部分完成 | `LocalWorkspace` + `DockerWorkspace` CLI 完整；**`E2BWorkspace` FS/命令/生命周期 ✅**（`e2b.go` ~519 行，25 测试）；缺 MCP Gateway 集成 |
| **权限引擎** | ✅ 已完成 | `permission.Engine` + glob/regex/substring 规则 + 4 种决策模式 |
| **Agent Middleware** | ✅ 已完成 | `middleware/` 五钩子洋葱链 + `LoggingMiddleware` 示例 + `HookOnError` |
| **Toolkit 中间件** | ✅ 已完成 | `toolkit.Middleware` 工具执行层：Logging + Metrics + Permission + Tracing + Offload |
| **MCP 集成** | ✅ 已完成 | Client + Server 适配器 + `reset_equipped_tools` + 动态注册 |
| **多租户 Service** | ✅ 已完成 | `service/` CRUD + Auth + RedisStorage + AES-GCM 加密 |
| **Gateway Session Persistence** | ✅ 已完成 | `SessionStateManager` + SSE/WS handler 集成 + Resume handler + Redis 后端 |
| **Message & Event 双模态系统** | ✅ 已完成 | `Msg.AppendEvent()` + `FinishedAt/Usage` + `ModelCallEndEvent` token 计数 + Storage `UpsertMessage` + SessionManager 自动持久化 |
| **Scheduler / Cron** | ✅ 已完成 | 基于 `robfig/cron/v3`，支持 `Schedule/Cancel/NextRun/duplicate ID 替换` |
| **A2A 协议** | ✅ 已完成 | AgentCard / Task / SSE / Cancel / V2Adapter / Registry 健康检查 |
| **Gateway** | ✅ 已完成 | HTTP + SSE `/v2/chat/stream` + WebSocket `/v2/chat/ws` + 多租户认证 |
| **OpenTelemetry** | ✅ 已完成 | `InitTracerProvider` + Gateway HTTP 追踪中间件 |
| **LangSmith 可观测性** | ✅ 已完成 | `LangSmithClient` + `LangSmithObserver` + event.Bus 订阅 |
| **ReMe Memory** | 🏆 **领先** | ReMeFile + ReMeVector + 5 向量后端 + Hybrid Search + Orchestrator + BM25/FTS5 |
| **多 Agent 编排** | ✅ 完整 | Pipeline / MsgHub / Workflow / Reflection / MapReduce / Parallel |
| **异步任务池** | ✅ 已完成 | `async.Pool` 固定 goroutine 池 + 任务状态跟踪 + 优雅关闭 |
| **文档加载器** | ✅ 已完成 | `Loader` interface + `TextLoader` + `DirLoader` |
| **子 Agent 工具** | ✅ 已完成 | `SubagentTool` 支持递归调用 + 嵌套深度限制 |
| **跨语言契约测试** | ✅ 已完成 | Go ↔ Python 双向 JSON 解析验证 |
| **结构化输出** | ✅ 已完成 | `ResponseFormat` + `json_object` / `json_schema` |
| **内置工具** | ✅ 已完成 | Read/Write/Edit/Glob/Grep/Shell/Subagent/ResetTools；缺 Task 工具 + SkillViewer |
| **OpenAI Response API** | ✅ 已完成 | `model/openai_response/` 自定义 HTTP client + SSE 流式解析 |
| **上下文压缩** | ✅ 已完成 | `memory/compactor.go` + `tool_result_compactor.go`，ReAct loop 集成 |
| **外部工具执行** | ⚠️ 协议就绪 | 事件类型 + `InjectEvent` 支持；ReAct loop 未识别 `is_external_tool` 标记 |

### 3.2 领先领域

| 领域 | 说明 |
|------|------|
| **Memory 系统** | ~6,025 行，37 个测试文件。ReMe 长期记忆、向量检索、混合搜索（向量 + BM25/FTS5）、5 种向量后端、EmbeddingCache、Orchestrator 编排器——全面领先 PyV2（无独立 memory 包） |
| **A2A 协议** | `a2a/` ~826 行，AgentCard / Task / SSE / V2Adapter / Registry——**PyV2 roadmap 尚未实现** |
| **SubagentTool** | `tool/subagent/` 支持递归 Agent 调用 + 嵌套深度限制——**PyV2 无 subagent 抽象** |
| **并发性能** | 利用 Go 的 goroutine + channel 实现真并行工具执行、事件流背压自然由 channel 缓冲处理 |
| **类型安全** | 强类型事件体系、interface 驱动的 Formatter/Model/Workspace 抽象，编译期保证 |
| **E2B Workspace** | envd Connect-RPC 完整 FS/命令实现（2026-06 追平），PyV2 仍领先 MCP Gateway 集成 |
| **服务化架构** | Gateway（HTTP/SSE/WS）+ Service（Storage/Auth）+ Schedule 拆分，比 PyV2 `app/` 一体化架构更解耦 |
| **AudioModel 预留** | `model/audio.go` 已预留 `AudioModel` 接口 + `OpenAITTS` 实现，为 Voice Agent 提前布局 |
| **异步任务池** | `async.Pool` 为后台长任务提供统一执行抽象 |
| **文档加载器** | `loader/` 为 RAG 管道提供标准化文档加载，PyV2 无对等模块 |
| **模型后端数量** | 10 个（含 vLLM），PyV2 为 9 个（无 vLLM）|
| **示例生态** | 21 个 Go examples vs PyV2 2 个顶层 examples（但 PyV2 含完整 web_ui）|

### 3.3 关键滞后领域（第 5 版更新）

| 领域 | 现状 | 与 PyV2 差距 | 优先级 |
|------|------|-------------|--------|
| **AG-UI Protocol** | ❌ 缺失 | PyV2 `app/` 原生 AG-UI 中间件，Studio UI 即插即用 | **P2** |
| **Task 内置工具** | ❌ 缺失 | PyV2 `tool/_task/` 含 Create/Get/List/Update 4 个任务工具 | **P2** |
| **Schedule Agent 工具** | ❌ 缺失 | PyV2 `ScheduleCreate/List/Stop/View` 4 个 Agent 可调用工具 | **P2** |
| **SkillViewer 工具** | ❌ 缺失 | PyV2 内置 Skill 查看器；Go 仅有 `load_skill_through_path` | P2 |
| **MCP Gateway** | ❌ 缺失 | PyV2 沙箱内 FastAPI MCP Gateway + `GatewayMCPTool` HTTP 代理 | **P2** |
| **Bash AST 解析器** | ⚠️ 启发式 | PyV2 tree-sitter-bash ~883 行；Go `permission/safety.go` 子串/前缀匹配 | P2 |
| **文件读缓存** | ❌ 缺失 | PyV2 ToolContext LRU（100 文件/25KB）；Go 无 Read 工具缓存 | P2 |
| **Offloader 协议** | ⚠️ 简化 | PyV2 `Offloader` 正式接口；Go 仅有 `OffloadMiddleware` | P2 |
| **外部工具执行** | ⚠️ 未接线 | PyV2 `is_external_tool` 全流程；Go 事件类型就绪但未触发 | P2 |
| **Tool Offload 中间件** | ❌ 缺失 | PyV2 app 层长耗时工具自动转后台任务 | P2 |
| **ModelCard / YAML** | ❌ 缺失 | PyV2 33 个 YAML ModelCard + `/model` API | P3 |
| **Formatter 多 Agent** | ⚠️ 5/9 后端 | PyV2 9 后端各含 MultiAgent Formatter | P2 |
| **Docker Workspace** | ⚠️ 基础 | PyV2 1,229 行含 Dockerfile 模板生成；Go ~215 行 CLI 包装 | P2 |
| **E2B MCP 集成** | ⚠️ 部分 | PyV2 E2B + MCP Gateway 一体化；Go E2B FS/命令 ✅，缺 Gateway | P2 |
| **Web UI 示例** | ❌ 缺失 | PyV2 `examples/web_ui/` 含完整 React 前后端 | P3 |
| **Background Task Manager** | ⚠️ 简化 | PyV2 334 行完整管理器；Go ~151 行基础版 | P2 |
| **开发者文档** | ✅ 骨架已扩充 | Go 5 文件 ~1,200 行；PyV2 docs.agentscope.io 更完整 | P2 |
| **端到端多租户示例** | ✅ 已存在 | `examples/multi_tenant_workspace/` 编译通过，缺少运行验证文档 | P2 |

---

## 4. 差距分析矩阵

将差距按 **架构影响 × 实施成本** 划分为四个象限，并标注当前完成状态：

```
            高架构影响
                 │
    ┌────────────┼────────────┐
    │  P0 核心   │  P1 生产   │
    │  事件重构  │  能力补齐  │
    │  ✅ 已完成  │  ✅ 基本完成│
    │            │            │
低实施成本 ──────┼────────────┤ 高实施成本
    │            │            │
    │  P2 服务化 │  P3 前瞻   │
    │  🟡 具雏形 │  🔵 持续   │
    │  新增差距  │            │
    └────────────┼────────────┘
            低架构影响
```

### P0 — 核心架构重构（✅ 已完成）

| 编号 | 项目 | 状态 | 说明 |
|------|------|------|------|
| P0-1 | **细粒度事件体系** | ✅ 已完成 | 20+ 事件类型，JSON 与 PyV2 互解析，`event.Bus` + `MetricsHandler` |
| P0-2 | **真事件流 `ReplyStream`** | ✅ 已完成 | `<-chan event.AgentEvent`，支持模型增量、工具调用生命周期、挂起/恢复 |
| P0-3 | **AgentState 状态机** | ✅ 已完成 | `reply_id`, `cur_iter`, 可序列化、可持久化、`InjectEvent` 恢复 |
| P0-4 | **挂起-恢复协议** | ✅ 已完成 | `RequireUserConfirm` / `ExternalExecutionResult` 事件，HITL 闭环 |

### P1 — 生产级能力补齐（✅ 基本完成）

| 编号 | 项目 | 状态 | 说明 |
|------|------|------|------|
| P1-1 | **Workspace 抽象** | ✅ 大部分完成 | `LocalWorkspace` + `DockerWorkspace` ✅；**`E2BWorkspace` FS/命令/生命周期 ✅**；缺 MCP Gateway |
| P1-2 | **权限引擎** | ✅ 已完成 | `Engine` + `Rule` + glob/regex/substring 匹配 + 4 决策模式 |
| P1-3 | **Toolkit 中间件链** | ✅ 已完成 | 洋葱模式 5 个 hook 点，内置 4 个中间件 + Tracing + Offload |
| P1-4 | **MCP 深度集成** | ✅ 已完成 | Server 适配器 + `fullSession` + `RegisterMCPManager` + `reset_equipped_tools` |
| P1-5 | **Context Compression 自动集成** | ✅ 已完成 | `ReActAgent.buildHistory` 接入 `PreReasoningPrepare` |
| P1-6 | **元工具 `reset_equipped_tools`** | ✅ 已完成 | Agent 运行时动态切换工具组 |
| P1-7 | **ThinkingBlock/HintBlock 深度使用** | ✅ 已完成 | `formatter/` 自动提取 `<think>` 标签，`message/` 支持 `HintBlock` |
| P1-8 | **State Store 生产级后端** | ✅ 已完成 | `JSONFileStore` + `RedisStore`（TTL + SCAN + prefix） |
| P1-9 | **Credential AES-GCM 加密** | ✅ 已完成 | `service.Cipher` + `NewCipherFromEnv()` + Gateway 创建时自动加密 |
| P1-10 | **SubagentTool** | ✅ 已完成 | `tool/subagent/subagent.go`，支持 Stream/Call 模式 + 嵌套深度限制 |
| P1-11 | **内置工具缺口** | ✅ 已完成 | Edit/Glob/Grep + Read/Write/Shell/Subagent/ResetTools 已全部实现 |

### P2 — 服务化与生态扩展（🟡 基本完成，存在新增差距）

| 编号 | 项目 | 状态 | 说明 |
|------|------|------|------|
| P2-1 | **多租户 Service 层** | ✅ 已完成 | `service/` CRUD + Auth + RedisStorage + AES-GCM 加密 |
| P2-2 | **Storage 抽象 + Redis** | ✅ 已完成 | `Storage` interface + `MemoryStorage` + `RedisStorage` |
| P2-3 | **Scheduler / Cron** | ✅ 已完成 | `Schedule/Cancel/ListJobs` 基于 `robfig/cron/v3`，支持重复 ID 替换 |
| P2-4 | **A2A / A2UI 增强** | ✅ 已完成 | V2 适配器 + Registry 健康检查 + 动态发现 |
| P2-5 | **OpenTelemetry 深度集成** | ✅ 已完成 | `InitTracerProvider` + Gateway HTTP 追踪中间件 |
| P2-6 | **Gateway Session Persistence** | ✅ 已完成 | `SessionStateManager` + SSE handler `SaveSnapshot` + Resume handler + WS handler 集成 |
| P2-7 | **LangSmith 可观测性** | ✅ 已完成 | `LangSmithClient` + `LangSmithObserver` 订阅 event.Bus |
| P2-8 | **异步任务池** | ✅ 已完成 | `async.Pool` 固定 goroutine 池 + 任务状态跟踪 |
| P2-9 | **文档加载器** | ✅ 已完成 | `Loader` interface + `TextLoader` + `DirLoader` |
| P2-10 | **Pipeline 并行执行** | ✅ 已完成 | `pipeline.Parallel` 实现 `agent.Agent` + 自定义聚合 |
| P2-11 | **端到端多租户示例** | ✅ 已完成 | `examples/multi_tenant_workspace/` 编译通过 |
| P2-12 | **跨语言契约测试** | ✅ 已完成 | `tests/cross_lang/` Go ↔ Python 双向 JSON 解析验证 |
| P2-13 | **结构化输出** | ✅ 已完成 | `model.ResponseFormat` + `json_object` / `json_schema` |
| **P2-14** | **AG-UI Protocol** | ❌ **新增差距** | PyV2 `app/_middleware/_protocol/_agui.py` 原生支持；Go 需自行转换事件流 |
| **P2-15** | **Task 内置工具** | ❌ **新增差距** | PyV2 `tool/_task/` 4 个任务管理工具；Go 无对等实现 |
| **P2-16** | **SkillViewer 工具** | ❌ **新增差距** | PyV2 内置 Skill 查看器；Go `skill/` 包存在但无对应内置工具 |
| **P2-17** | **Offloader 协议** | ⚠️ **新增差距** | PyV2 正式 `Offloader` 接口；Go 仅有 `OffloadMiddleware`（功能子集）|
| **P2-18** | **Background Task Manager** | ⚠️ **新增差距** | PyV2 334 行完整管理器；Go ~151 行基础版，缺少任务状态机与重试 |
| **P2-19** | **MCP Gateway** | ❌ **第 5 版新增** | PyV2 `workspace/_mcp_gateway/` 沙箱内 MCP HTTP 代理；Go MCP 仅库级 |
| **P2-20** | **Schedule Agent 工具** | ❌ **第 5 版新增** | PyV2 4 个 Schedule 工具；Go 仅有 HTTP `/schedule` API |
| **P2-21** | **Bash AST 解析器** | ⚠️ **第 5 版新增** | PyV2 tree-sitter-bash；Go 启发式 `permission/safety.go` |
| **P2-22** | **文件读缓存** | ❌ **第 5 版新增** | PyV2 ToolContext LRU；Go Read 工具无 mtime 缓存 |
| **P2-23** | **外部工具执行接线** | ⚠️ **第 5 版新增** | 事件类型就绪，ReAct loop 未识别 external tool 标记 |
| **P2-24** | **Tool Offload 中间件** | ❌ **第 5 版新增** | PyV2 app 层长耗时工具后台化；Go 无对等实现 |
| **P2-25** | **Formatter 多 Agent** | ⚠️ **第 5 版新增** | PyV2 9 后端 MultiAgent Formatter；Go 5 后端无 MultiAgent |

### P3 — 前瞻布局（🔵 持续迭代）

| 编号 | 项目 | 状态 | 说明 |
|------|------|------|------|
| P3-1 | **Voice Agent 预备接口** | 🟡 已预留 | `AudioModel` interface + `OpenAITTS` 实现 |
| P3-2 | **Agentic RL 接口** | 🔵 未开始 | 与 Tinker 等后端对接预留 |
| P3-3 | **模型路由/降级/重试** | ✅ 已完成 | `model/router` 重试/回退/退避路由 |
| P3-4 | **OpenAI Response API** | ✅ 已完成 | `model/openai_response/` 已落地 |
| **P3-5** | **ModelCard / YAML 定义** | ❌ **新增差距** | PyV2 `model/_model_card.py` 声明式模型配置 |
| **P3-6** | **Web UI 示例** | ❌ **新增差距** | PyV2 `examples/web_ui/` 完整前后端参考实现 |
| **P3-7** | **Docker Workspace 深度** | ⚠️ **新增差距** | Dockerfile 模板生成、多阶段构建、依赖安装 |
| **P3-8** | **E2B MCP Gateway 集成** | ⚠️ **第 5 版修正** | E2B FS/命令 ✅ 已实现；剩余 MCP Gateway 沙箱内集成 |

---

## 5. V2 演进总体设计

### 5.1 设计原则

1. **范式优先**：P0 事件驱动范式重构已完成，所有后续功能建立在正确的地基上。
2. **Go 地道性**：利用 Go 的 `interface`、`struct embedding`、`channel`、`goroutine`、`context.Context`，而非简单翻译 Python 的 asyncio。
3. **最小侵入**：保持 `Agent` interface 不变（或仅增加方法），`ReActAgent` 通过 embedding 复用新能力，现有用户代码不 break。
4. **优势放大**：Memory 系统继续领先，事件流与 Memory 的 `PreReasoningPrepare` 深度整合，形成差异化竞争力。
5. **跨语言互操作**：事件序列化格式、Msg JSON、A2A 协议与 PyV2 保持一致，确保 Studio UI 可以对接 Go 后端。

### 5.2 总体架构图（V2 当前态 → 目标态）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Application / Service Layer                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │  HTTP API   │ │  SSE Stream │ │  WebSocket  │ │  Scheduler / Cron   │   │
│  │  (Multi-    │ │  (AgentEvent│ │  (Bi-di     │ │  (Background Task)  │   │
│  │   tenant)   │ │   Stream)   │ │   Stream)   │ │                     │   │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └─────────────────────┘   │
│         │               │               │         ┌─────────────────────┐   │
│         │               │               │         │  AG-UI Protocol 🆕  │   │
│         │               │               │         │  (待实现)            │   │
│         └───────────────┴───────────────┴─────────┴─────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│         │               │               │         Orchestration Layer      │
│  ┌──────┴───────────────┴───────────────┴──────┐  Pipeline / MsgHub / WF   │
│  │              Agent Service Layer             │  Reflection / MapReduce   │
│  │  ┌──────────┐ ┌──────────┐ ┌────────────┐  │  Parallel / A2A          │
│  │  │ Session  │ │  State   │ │Credential  │  │                           │
│  │  │ Manager  │ │  Store   │ │  Manager   │  │                           │
│  │  └──────────┘ └──────────┘ └────────────┘  │                           │
│  └─────────────────────────────────────────────┘                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                           Agent Core Layer (V2 已完成)                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         AgentState (P0-3) ✅                         │   │
│  │  reply_id | cur_iter | cur_summary | context | tool_ctx | perm_ctx   │   │
│  │  Load() / Save() / Serialize() / Deserialize()                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Event Stream Engine (P0-1/2) ✅                   │   │
│  │  <-chan AgentEvent  ──►  Block Lifecycle  ──►  HITL State Machine   │   │
│  │  TextBlockDelta | ToolCallDelta | RequireUserConfirm | ...          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────┐  ┌────────────────────────────────────────┐  │
│  │   ReActAgent (embedding) │  │   Middleware Chain (P1-3) ✅            │  │
│  │   ├─ reasoning loop      │  │   ├─ on_reply                         │  │
│  │   ├─ acting loop         │  │   ├─ on_reasoning                     │  │
│  │   ├─ context compression │  │   ├─ on_acting                        │  │
│  │   └─ state management    │  │   ├─ on_model_call                    │  │
│  │                          │  │   └─ on_system_prompt                 │  │
│  └──────────────────────────┘  └────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│                         Capability Layer                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐    │
│  │   Model     │  │   Toolkit   │  │   Memory    │  │   Workspace     │    │
│  │ (10 后端)   │  │ +🆕Task/Sched│  │ (ReMe 领先) │  │(Local/Docker/   │    │
│  │             │  │ +🆕SkillView │  │             │  │  E2B ✅ +🆕GW)  │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │   Permission Engine (P1-2) ✅                                         │   │
│  │   Rule-based: ALLOW | DENY | ASK | PASSTHROUGH                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │   Offloader Protocol (P2-17) 🆕 待补齐                                │   │
│  │   大上下文 / 工具结果外部存储卸载接口                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 第一阶段：事件驱动范式重构（P0）— 已完成

> **实际工期**：2026-04-15 至 2026-05-15（约 4 周）  
> **状态**：✅ **全部交付并验收通过**

P0 是 V2 的**地基工程**。Go 版利用 `channel` + `goroutine` + `select` 的天然优势，完成了从"消息为中心"到"事件为中心"的范式转移。

### 6.1 交付物总结

| 验收项 | 状态 | 实际交付 |
|--------|------|----------|
| 事件类型覆盖 | ✅ | `event/` 包 20+ 事件类型，与 PyV2 事件 JSON 字段完全对齐（见附录 A） |
| 流式完整性 | ✅ | `ReplyStream()` 真流式消费模型输出、工具调用、工具结果；`Call()` 和 `CallStream()` 保留，基于 `ReplyStream` 包装实现 |
| 挂起恢复 | ✅ | `RequireUserConfirmEvent` 发出后，Agent 进入 `SUSPENDED`，`InjectEvent()` 注入 `UserConfirmResultEvent` 恢复执行 |
| 状态序列化 | ✅ | `state.AgentState` JSON 序列化/反序列化精确恢复对话位置；`JSONFileStore` + `RedisStore` 已实现 |
| 向后兼容 | ✅ | `Call()` / `CallStream()` 接口保留，内部复用 `ReplyStream` |
| 测试覆盖 | ✅ | `event/` 包 90%+ 覆盖率；挂起恢复集成测试通过 |

### 6.2 关键技术决策（已验证）

1. **Channel vs Iterator**：Go 使用 `<-chan event.AgentEvent` 天然适合事件流，比 Python 的 `AsyncGenerator` 更地道，背压由 channel 缓冲和 `context.Context` 自然处理。
2. **挂起-恢复机制**：内部维护 `pendingExternalEvents map[string]chan event.AgentEvent`，`RequireUserConfirmEvent` 发出后 loop goroutine 阻塞在对应 chan 上，外部调用 `InjectEvent()` 恢复。验证可行。
3. **ReplyID 追踪**：每个 `ReplyStream()` 生成 UUID，所有子事件携带该 ID，便于追踪和状态恢复。

---

## 7. 第二阶段：生产级能力补齐（P1）— 基本完成

> **实际工期**：2026-05-15 至 2026-06-07（约 3 周）  
> **状态**：✅ **核心能力已交付**

在 P0 事件驱动地基上，Go 版完成了 Workspace、权限、Toolkit 中间件、MCP、Formatter、State Store、Credential 加密、SubagentTool 等生产级能力的密集补齐。

### 7.1 已交付核心能力

| 模块 | 交付内容 | 测试 |
|------|---------|------|
| `workspace/` | `LocalWorkspace` + `DockerWorkspace` CLI + **`E2BWorkspace` 完整 FS/命令** | 25+ E2B 测试 |
| `permission/` | 规则引擎（glob/regex/substring）+ 4 种决策模式 | 19 个测试 |
| `middleware/` | Agent 级 `MiddlewareBase` 对齐：`on_reply/on_reasoning/on_acting/on_model_call/on_system_prompt` + `HookOnError` | 6+ 测试 |
| `toolkit/middleware` | 工具执行层 Logging + Metrics + Permission + Tracing + Offload 洋葱链 | 10 个测试 |
| `toolkit/mcp` | MCP Server 适配器 + `fullSession` + `RegisterMCPManager` + `reset_equipped_tools` | 已有测试 |
| `formatter/` | `ThinkingFormatter` + `extractThinkingBlocks` + 5 后端实现 | 8+ 测试 |
| `message/block` | `HintBlock` + JSON 序列化 + `GetHintContent` / `GetThinkingContent` | 已有测试 |
| `model/router` | 重试/回退/退避路由 | 8 个测试 |
| `model/audio` | `AudioModel` 接口 + `MockAudioModel` + `OpenAITTS` | 6 个测试 |
| `state/redis_store` | Redis 后端 + TTL + SCAN + prefix | 已有测试 |
| `service/cipher` | AES-256-GCM + `NewCipherFromEnv()` | 已有测试 |
| `tool/subagent` | `SubagentTool` + 嵌套深度限制 | 已有测试 |
| `pipeline/parallel` | 并发 Agent 执行 + 自定义聚合 | 7 个测试 |
| `async/pool` | 固定 goroutine 池 + 任务跟踪 + 优雅关闭 | 6 个测试 |
| `loader/` | `TextLoader` + `DirLoader` | 4 个测试 |
| `tool/file/` | Read/Write/Edit/Glob/Grep + Shell + Subagent | 30+ 个测试 |
| `model/openai_response` | Response API + SSE 流式解析 | 6 个测试 |

### 7.2 剩余缺口（按优先级）

#### 1. `workspace/docker.go` — Dockerfile 生成与容器生命周期（P1-1 剩余）

- **现状**：`docker cp` + `docker exec` 操作已有容器；无 `docker run` 生命周期、Dockerfile 模板生成
- **PyV2 参考**：`_docker/_make_dockerfile.py` + 5 种 Dockerfile 模板
- **缓解措施**：生产环境可先用 `E2BWorkspace`（Go 已完整实现 FS/命令）或手动管理 Docker 容器

#### 2. MCP Gateway 沙箱内集成（P2-19）

- **现状**：Go `toolkit/mcp/` 提供 Client/Server 库级集成；无 PyV2 的 `workspace/_mcp_gateway/` HTTP 代理
- **影响**：Docker/E2B 沙箱内 MCP 无法通过 Gateway HTTP 端点远程调用

---

## 8. 第三阶段：服务化与生态扩展（P2）— 基本完成，存在新增差距

> **状态**：✅ **核心服务已交付，本次对比新发现 5 项差距需补齐**

### 8.1 已交付核心能力

| 模块 | 交付内容 | 测试 |
|------|---------|------|
| `service/` | `Storage` interface + `MemoryStorage` + `RedisStorage` + Auth + CRUD + AES-GCM | 17+ 测试 |
| `schedule/` | Cron 调度器 `Schedule/Cancel/NextRun` + 重复 ID 替换修复 | 8+ 测试 |
| `gateway/` | V2 SSE `/v2/chat/stream` + WebSocket `/v2/chat/ws` + 多租户认证 + Session State Persistence | 5+ 测试 |
| `a2a/` | 动态发现 `Registry` + V2 适配器 + 健康检查 | 8+ 测试 |
| `observability/` | `InitTracerProvider` + `OtelTracer` + `LangSmithClient` + `LangSmithObserver` | 4+ 测试 |
| `event/metrics` | `MetricsHandler` HTTP endpoint，暴露 `MetricsSnapshot` JSON | 2+ 测试 |
| `gateway/session_state` | `SaveSnapshot` + `LoadSnapshot` + `Resume` + `DeleteSnapshot` | 11 个测试 |
| `async/pool` | 异步任务执行池 | 6 个测试 |
| `loader/` | 文档加载器 | 4 个测试 |
| `pipeline/parallel` | 并发 Agent 执行 | 7 个测试 |
| `tests/cross_lang` | Go ↔ Python 双向 JSON 契约测试 | 通过 |

### 8.2 Gateway Session State Persistence 已闭环

此前标记为"未闭环"的架构级缺口已完全解决：

- `gateway/sse.go`：当 Agent 因 `RequireUserConfirmEvent` 挂起时，SSE handler 自动调用 `s.sessionState.SaveSnapshot()`
- `gateway/ws.go`：WebSocket handler 同样集成 Save/Load/Resume
- `gateway/resume_handler.go`：`/v2/resume` 端点调用 `s.sessionState.Resume()` 加载快照并注入恢复事件
- `gateway/server.go`：Server 自动创建 `SessionStateManager`，若配置了 Storage 则启用持久化

### 8.3 本次对比新发现的 P2 差距（需纳入收尾）

#### 8.3.1 AG-UI Protocol（P2-14）

- **PyV2 状态**：`app/_middleware/_protocol/_agui.py` (257 行) 实现 `AGUIProtocolMiddleware`，将 AgentScope 事件自动转换为 AG-UI 协议事件，支持 `ReasoningMessageStart/End`、`ToolCallStart/End` 等前端友好格式
- **Go 版状态**：❌ 缺失。Gateway 直接输出原始 `AgentEvent` JSON，前端需自行解析
- **影响**：使用 PyV2 官方 Studio UI 连接 Go 后端时，需要额外适配层；无法即插即用
- **建议方案**：
  1. 新增 `gateway/agui.go`：实现 `AGUIProtocolMiddleware`，将 `AgentEvent` 转换为 AG-UI 事件格式
  2. 提供 `gateway.WithAGUI()` 选项，启用后 SSE/WS 流自动转换
  3. 参考 PyV2 的 `_to_agui_event` 映射逻辑（见附录 E）

#### 8.3.2 Task 内置工具（P2-15）

- **PyV2 状态**：`tool/_task/` 含 4 个工具：
  - `TaskCreate` — 创建后台任务
  - `TaskGet` — 查询任务状态
  - `TaskList` — 列出任务
  - `TaskUpdate` — 更新任务
- **Go 版状态**：❌ 缺失。`async/pool` 提供底层执行能力，但 Agent 无法通过工具调用自管理任务
- **影响**：Agent 无法创建长时间运行的后台任务并跟踪其状态
- **建议方案**：
  1. 新增 `tool/task/` 包，实现 `TaskCreateTool`, `TaskGetTool`, `TaskListTool`, `TaskUpdateTool`
  2. 底层复用 `async.Pool`，上层暴露为 FunctionTool
  3. 与 `gateway/background_task.go` 打通，统一任务 ID 与状态查询

#### 8.3.3 SkillViewer 内置工具（P2-16）

- **PyV2 状态**：`tool/_builtin/_skill.py` 实现 `SkillViewer`，Agent 运行时可通过工具调用查看已加载的 `Skill` 详情（markdown 内容）
- **Go 版状态**：❌ 缺失。`skill/` 包存在（`skill.go`, `loader.go`, `registry.go` 等），但无对应内置工具暴露给 Agent
- **影响**：Agent 无法感知自身具备哪些 Skill，无法按需调用领域知识
- **建议方案**：
  1. 新增 `tool/skillviewer.go`：实现 `SkillViewerTool`，查询 `skill.Registry`
  2. 在 `Toolkit` 初始化时自动注入（当 `skill` 不为空时）

#### 8.3.4 Offloader 协议（P2-17）

- **PyV2 状态**：`workspace/_offload_protocol.py` 定义正式 `Offloader` Protocol：
  - `offload_context(session_id, msgs) -> str`：将压缩后的上下文卸载到外部存储
  - `offload_tool_result(session_id, tool_result) -> str`：将大型工具结果卸载到外部存储
- **Go 版状态**：⚠️ 仅有 `OffloadMiddleware`（`toolkit/middleware.go` 第 178 行），将工具结果写入 Workspace 本地文件，无统一接口
- **影响**：大上下文场景下（如长文档处理），AgentState 膨胀；大型工具结果（如图片、视频）无法统一外存
- **建议方案**：
  1. 新增 `workspace/offloader.go`：定义 `Offloader` interface
  2. 实现 `WorkspaceOffloader`（基于现有 Workspace）和 `RedisOffloader`（基于 Redis）
  3. 在 `Middleware` 中替换现有 `OffloadMiddleware`，改为调用正式 `Offloader`

#### 8.3.5 Background Task Manager（P2-18）

- **PyV2 状态**：`app/_manager/_background_task_manager.py` (334 行) 完整实现：
  - 任务状态机：`PENDING → RUNNING → COMPLETED / FAILED / CANCELLED`
  - 任务重试、超时、取消
  - 与 SessionManager 集成
- **Go 版状态**：⚠️ `gateway/background_task.go` (174 行) 基础实现，仅支持创建和列表查询
- **影响**：生产环境中后台任务缺乏完整生命周期管理
- **建议方案**：
  1. 扩展 `gateway/background_task.go`：增加任务状态机、重试策略、超时控制
  2. 与 `async.Pool` 深度集成，Pool 负责执行，Manager 负责状态跟踪

### 8.4 P2 阶段验收标准

| 验收项 | 目标 | 实际状态 |
|--------|------|----------|
| 多租户 | 支持多用户注册，用户数据隔离 | ✅ API 已实现 + 端到端示例编译通过 |
| 会话持久化 | Session + AgentState 可保存到 Redis，跨请求恢复 | ✅ `gateway/session_state.go` 已闭环 |
| 定时任务 | 支持 Cron 表达式调度 Agent 执行 | ✅ 完全达成 |
| Studio UI 兼容 | PyV2 Studio UI 可以连接 Go 后端并正常显示事件流 | ⚠️ 协议对齐 + 跨语言契约测试通过，但缺少 AG-UI 中间件 |
| 可观测性 | LangSmith + OpenTelemetry 双链路追踪 | ✅ 完全达成 |

---

## 9. 关键落后项深度分析（更新）

本节聚焦 Go 版相对于 PyV2 **当前仍存在的实质性差距**，按影响程度排序。

### 9.1 AG-UI Protocol（影响：高）

- **PyV2 状态**：`app/_middleware/_protocol/_agui.py` 实现完整的 AG-UI 协议中间件，将 20+ 种 `AgentEvent` 映射为前端友好的 `AGUIBaseEvent`
- **Go 版状态**：❌ 缺失。Gateway 直接序列化原始 `AgentEvent`
- **后果**：
  - 使用 PyV2 官方前端（Studio UI）时，Go 后端无法直接对接
  - 前端开发者需自行理解 AgentScope 事件格式，adoption 门槛提高
- **建议行动**：
  1. 定义 `gateway/agui.go`：`AGUIConverter` interface + `DefaultAGUIConverter`
  2. 实现事件映射：`TextBlockDeltaEvent` → `AGUIMessageContentEvent`，`ToolCallStartEvent` → `AGUIToolCallStartEvent`...
  3. 在 SSE/WS handler 中增加 `?protocol=agui` 查询参数切换

### 9.2 Task 内置工具（影响：中高）

- **PyV2 状态**：`tool/_task/` 含 `TaskCreate`, `TaskGet`, `TaskList`, `TaskUpdate` 四个工具，Agent 可以自管理后台任务
- **Go 版状态**：❌ 缺失。`async.Pool` 仅为底层执行抽象，无工具层暴露
- **后果**：
  - Agent 无法创建长时间运行的后台任务（如批量文件处理、定时数据同步）
  - 用户需手动调用 Gateway API 创建任务，破坏 Agent 自治性
- **建议行动**：
  1. `tool/task/create.go`：创建任务，返回 task ID
  2. `tool/task/get.go`：查询任务状态与结果
  3. `tool/task/list.go`：按 session 过滤列出任务
  4. `tool/task/update.go`：取消或修改任务参数

### 9.3 SkillViewer 内置工具（影响：中）

- **PyV2 状态**：`tool/_builtin/_skill.py` 实现 `SkillViewer`，Agent 调用 `"skill": "数据分析"` 即可查看对应 Skill 的 markdown 说明
- **Go 版状态**：❌ 缺失
- **后果**：Agent 无法感知可用 Skill，领域知识扩展能力受限
- **建议行动**：
  1. 新增 `tool/skillviewer.go`
  2. 绑定 `skill.Registry`，支持按名称查询 Skill 内容

### 9.4 Offloader 协议（影响：中）

- **PyV2 状态**：`workspace/_offload_protocol.py` 定义正式接口，支持上下文和工具结果的外存卸载
- **Go 版状态**：⚠️ `OffloadMiddleware` 仅支持工具结果写入 Workspace 本地目录
- **后果**：
  - Redis 存储的 AgentState 仍可能因大工具结果膨胀
  - 缺乏统一的外存抽象（如 S3、GCS、Redis 等）
- **建议行动**：
  1. 定义 `workspace.Offloader` interface
  2. 实现 `FileSystemOffloader`, `RedisOffloader`
  3. 替换现有 `OffloadMiddleware`

### 9.5 Docker Workspace 深度（影响：中）

- **PyV2 状态**：1,229 行，含 Dockerfile 模板生成、多阶段构建、PyPI/源码安装、Node 复制、健康检查
- **Go 版状态**：~215 行，仅 CLI 包装（`docker cp` + `docker exec` 操作已有容器）
- **后果**：
  - 无法自动生成含依赖的 Dockerfile
  - 不支持容器内多阶段构建优化
  - 无容器健康检查与自动重启
- **建议行动**：
  1. 参考 PyV2 `_docker_workspace.py` 的 Dockerfile 模板逻辑
  2. 实现 `DockerWorkspace.GenerateDockerfile(deps []string)`
  3. 增加健康检查端点

### 9.6 E2B Workspace（影响：低 — **第 5 版状态修正**）

- **PyV2 状态**：`workspace/_e2b/_e2b_workspace.py` 1,034 行，完整支持文件读写、命令执行、MCP Gateway 集成
- **Go 版状态**：✅ **`workspace/e2b.go` ~519 行已实现** — sandbox 生命周期 + envd Connect-RPC 的 `ReadFile`/`WriteFile`/`ListDir`/`MkdirAll`/`Stat`/`Execute`；25 个测试通过
- **剩余差距**：MCP Gateway 沙箱内 HTTP 代理（见 §9.7）
- **建议行动**：
  1. ~~补齐 E2B 文件/命令操作~~ ✅ 已完成
  2. 实现 `workspace/mcp_gateway.go`，对标 PyV2 `_mcp_gateway/_mcp_gateway_app.py`
  3. 在 E2B bootstrap 流程中自动启动 MCP Gateway

### 9.7 MCP Gateway（影响：中高 — **第 5 版新增**）

- **PyV2 状态**：`workspace/_mcp_gateway/_mcp_gateway_app.py` 提供沙箱内 FastAPI MCP Gateway（`/health`, `/mcps`, `/mcps/{name}/tools`, `/mcps/{name}/tools/{tool}`）；`GatewayMCPTool` 通过 HTTP 代理远程调用
- **Go 版状态**：❌ 缺失。MCP 集成停留在 `toolkit/mcp/` 库级 Client/Server
- **后果**：Docker/E2B 沙箱场景下，宿主 Agent 无法通过 HTTP 代理调用沙箱内 MCP 工具
- **建议行动**：
  1. 新增 `workspace/mcp_gateway.go`：轻量 HTTP MCP 代理服务
  2. 新增 `toolkit/mcp/gateway_client.go`：对标 PyV2 `GatewayClient`
  3. Docker/E2B Workspace 初始化时可选启动 Gateway sidecar

### 9.8 Bash AST 解析器（影响：中 — **第 5 版新增**）

- **PyV2 状态**：`tool/_builtin/_bash_parser.py` (~883 行) 基于 tree-sitter-bash，解析复合命令、管道、前缀，支持 read-only git 检测
- **Go 版状态**：⚠️ `permission/safety.go` + `engine.go` 使用 `strings.Fields` 分词 + 子串/前缀/glob/regex 启发式匹配
- **后果**：复杂 shell 命令（如 `cd /tmp && rm -rf *`）可能绕过权限检测
- **建议行动**：
  1. 评估引入 Go bash parser（如 tree-sitter Go binding 或自研简化 AST）
  2. 至少在 `permission/safety.go` 中增加 `&&`/`||`/`|` 管道拆分预处理

### 9.9 文件读缓存（影响：低中 — **第 5 版新增**）

- **PyV2 状态**：`state/_state.py` `ToolContext.read_file_cache` — LRU 缓存（默认 100 文件 / 25KB），mtime 校验
- **Go 版状态**：❌ 缺失。`tool/file/read_file.go` 每次直接读 Workspace
- **后果**：Agent 重复 Read 同一文件时浪费 token 和 I/O
- **建议行动**：
  1. 在 `agent/react/state.go` 或 `tool/file/` 增加 LRU 读缓存
  2. 与 Workspace `Stat` 联动 mtime 失效

### 9.10 ModelCard / YAML 定义（影响：低中）

- **PyV2 状态**：`model/_model_card.py` 支持 YAML 声明式模型配置，含 provider、endpoint、参数默认值
- **Go 版状态**：❌ 缺失。模型配置全代码化
- **后果**：新增模型需改代码重新编译，无法运行时动态加载模型配置
- **建议行动**：
  1. 定义 `model.ModelCard` struct
  2. 支持从 YAML/JSON 文件加载模型定义
  3. 与 `service.Credential` 打通，实现运行时模型注册

### 9.11 开发者文档体系（影响：中高）

- **PyV2 状态**：`docs/` 目录完整，含快速开始、API 参考、架构说明、部署指南
- **Go 版状态**：`docs/` 已有 5 个骨架文件（`index.md` + `concepts.md` + `deployment.md` + `api-reference.md` + `tutorial.md`）共 ~1,200 行，但内容仍然较薄
- **后果**：
  - 社区 adoption 仍有门槛——Go 开发者无法快速理解事件驱动范式
  - 与 PyV2 的跨语言团队协作困难——缺乏统一术语和概念映射
  - 生产部署缺乏官方指导——多租户配置、Redis 连接、Workspace 安全策略文档不完整
- **建议行动**：
  1. 扩充 `docs/` 内容，采用 MkDocs / Docusaurus / GitBook 等静态站点生成器
  2. 优先补齐：API 参考（每个包的接口说明）、完整教程（Step-by-step）、架构深度文档
  3. 与 PyV2 文档建立跨语言链接，统一术语表

---

## 10. 第四阶段：前瞻布局（P3）

> **目标**：补齐本次对比新发现的差距，同时为 Voice Agent、Agentic RL 等 PyV2 roadmap 中的方向预留接口，保持竞争力。
> **状态**：🔵 持续迭代，不设定 deadline

### 10.1 AG-UI Protocol 实现（P3-新增）

```go
package gateway

// AGUIConverter 将 AgentEvent 转换为 AG-UI 协议事件。
type AGUIConverter interface {
    Convert(event.AgentEvent) (map[string]any, error)
}

// DefaultAGUIConverter 实现完整的事件映射。
type DefaultAGUIConverter struct{}

func (c *DefaultAGUIConverter) Convert(e event.AgentEvent) (map[string]any, error) {
    switch ev := e.(type) {
    case *event.TextBlockDeltaEvent:
        return map[string]any{
            "type": "message_content",
            "delta": ev.Delta,
        }, nil
    // ... 其他事件类型映射
    }
}
```

### 10.2 Task 工具实现（P3-新增）

```go
package task

// TaskCreateTool 允许 Agent 创建后台任务。
type TaskCreateTool struct {
    pool *async.Pool
}

func (t *TaskCreateTool) Execute(ctx context.Context, input map[string]any) (*tool.Response, error) {
    // 调用 async.Pool 创建任务，返回 task ID
}
```

### 10.3 Voice Agent 预备接口（P3-1）

PyV2 roadmap 明确 Voice Agent 是重点（TTS → 多模态 → 实时多模态）。Go 版已提前预留：

```go
package model

// AudioModel 预留接口（已实现 Mock + OpenAITTS）
type AudioModel interface {
    ChatModel // 继承文本模型能力
    
    // TTS
    SynthesizeSpeech(ctx context.Context, text string, opts AudioOptions) ([]byte, error)
    
    // 实时音频流（WebSocket / RTC）—— 预留
    StreamAudio(ctx context.Context, audioStream <-chan []byte, textStream chan<- string) error
}
```

**下一步**：对接 WebRTC 库（如 `pion/webrtc`），实现实时语音对话的端到端示例。

### 10.4 模型路由/降级/重试（P3-3）

当前 `model/router.go` 已实现基础版本：

```go
type Router struct {
    Primary   ChatModel
    Fallback  ChatModel
    MaxRetries int
    RetryPolicy RetryPolicy
}
```

**下一步**：增加熔断器（Circuit Breaker）、基于 latency 的动态路由、模型成本估算。

---

## 11. 风险与缓解

| 风险 | 概率 | 影响 | 状态 | 缓解措施 |
|------|------|------|------|----------|
| **P0 事件重构引入大量 break change** | 中 | 高 | ✅ 已化解 | 保持 `Call/CallStream` 不变，`ReplyStream` 作为新增方法；实际迭代中未出现用户投诉 |
| **与 PyV2 事件格式频繁变化** | 中 | 中 | ✅ 已化解 | 已建立跨语言 JSON 字段对齐（附录 A）；PyV2 v2.0.0 后格式已稳定；契约测试通过 |
| **Workspace/E2B 引入外部依赖** | 低 | 中 | 🟡 缓解中 | Workspace 为 interface，E2B 作为可选实现；核心保持 LocalWorkspace 零依赖 |
| **Redis/多租户增加部署复杂度** | 中 | 低 | ✅ 已缓解 | Service 层纯 interface，单机部署可用 memory 实现；生产环境可选 Redis |
| **并发挂起-恢复引入死锁/泄漏** | 中 | 高 | ✅ 已化解 | 严格的 `context.Context` 传播；1,157 个测试覆盖超时/取消/关闭场景，全量通过 |
| **Memory 优势被 PyV2 追赶** | 低 | 中 | 🟡 持续投入 | ReMe 持续迭代（记忆版本控制、分布式记忆），保持差异化 |
| **Gateway Session Persistence 未闭环** | 高 | 高 | ✅ 已化解 | `gateway/session_state.go` + SSE/WS handler 集成 + Resume handler 已闭环 |
| **文档缺失导致社区增长停滞** | 高 | 中高 | 🟡 缓解中 | 已建立 `docs/` 骨架（5 文件 / ~1,200 行）；需持续扩充 API 参考与教程 |
| **AG-UI 缺失导致前端生态不兼容** | 高 | **高** | 🔴 **当前最大风险** | 优先实现 `gateway/agui.go`，对齐 PyV2 Studio UI |
| **MCP Gateway 缺失限制沙箱 MCP 场景** | 中 | 中高 | 🟡 **第 5 版新增** | 实现 `workspace/mcp_gateway.go` |
| **Task/Schedule 工具缺失限制 Agent 自治性** | 中 | 中 | 🟡 待补齐 | 实现 `tool/task/` + `tool/schedule/` |

---

## 12. 演进时间线与里程碑

### 实际进度与预测时间线

```
2026-04        2026-05        2026-06        2026-07        2026-08
   │              │              │              │              │
   ├──────────────┴──────────────┤              │              │
   │        第一阶段 P0          │              │              │
   │    事件体系 + 真事件流       │              │              │
   │    AgentState + 挂起恢复     │              │              │
   │              │              │              │              │
   │         ┌────┴────┐         │              │              │
   │         ▼         ▼         │              │              │
   │    Alpha 1    Alpha 2       │              │              │
   │   (事件体系)   (状态机)      │              │              │
   │              │              ├──────────────┤              │
   │              │              │   第二阶段 P1 收尾           │
   │              │              │  E2B REST / SubagentTool     │
   │              │              │  State RedisStore / Cipher   │
   │              │              │  Gateway Session Persistence │
   │              │              │  Model backends (4 new)      │
   │              │              │  LangSmith / Parallel / Pool │
   │              │              │       │      │              │
   │              │              │       ▼      ▼              │
   │              │              │    Beta 1   Beta 2          │
   │              │              │   (E2B)   (State+Session)   │
   │              │              │              │              │
   │              │              │    ✅ 已完成  ✅ 已完成      │
   │              │              │              │              │
   │              │              │              ├──────────────┤
   │              │              │              │  第三阶段 P2  │
   │              │              │              │  收尾 + RC    │
   │              │              │              │  AG-UI / Task │
   │              │              │              │  / SkillView  │
   │              │              │              │       │       │
   │              │              │              │       ▼       │
   │              │              │              │    RC 1       │
   │              │              │              │              │
   │              │              │              │              ▼
   │              │              │              │           v2.0.0
   │              │              │              │           Release
```

### 关键里程碑（更新版）

| 里程碑 | 预测时间 | 交付物 | 验收标准 |
|--------|----------|--------|----------|
| **Alpha 1** | ✅ 已完成 | `event/` 包 + `ReplyStream` 初版 | 20+ 事件类型定义，模型输出可流式消费 |
| **Alpha 2** | ✅ 已完成 | AgentState + 挂起恢复 | 支持用户确认挂起-恢复，状态可序列化 |
| **Beta 1** | ✅ 已完成 | E2B 完整 FS/命令 + SubagentTool + 4 新模型后端 | E2B 沙箱可创建/读写/执行命令，SubagentTool 可递归调用 Agent |
| **Beta 2** | ✅ 已完成 | State RedisStore + Gateway Session Persistence + LangSmith | 多副本 Gateway 共享 AgentState，跨请求挂起-恢复闭环 |
| **RC 1** | 🟡 2026-06 末 | AG-UI Protocol + Task 工具 + SkillViewer + 文档体系扩充 | Studio UI 可对接 Go 后端；Agent 可自管理任务；`docs/` API 参考上线 |
| **v2.0.0** | 🔵 2026-07 | 正式发布 | 全部验收标准通过，10+ 模型后端，跨语言契约测试通过，文档完整，AG-UI 兼容 |

---

## 13. 实施状态跟踪

> 本节随每次迭代更新，记录各模块的精确完成状态。

### ✅ 已完成（截至 2026-06-07）

| 阶段 | 模块 | 交付内容 | 测试 |
|------|------|---------|------|
| P0 | `event/` | 20+ 事件类型 + JSON 序列化 + 事件总线 `Bus` + `MetricsHandler` | 8+ 测试 |
| P0 | `agent/react` | `ReplyStream()` + `AgentState` 挂起恢复 + HITL 注入 + 并发工具执行 | 5+ 测试 |
| P0 | `model/` | `StreamChunk.IsThinking` + `ThinkingBlockDeltaEvent` + Router | 集成测试 |
| P1 | `workspace/` | `LocalWorkspace` + `DockerWorkspace` CLI + **`E2BWorkspace` 完整 FS/命令** | 25+ E2B 测试 |
| P1 | `permission/` | 规则引擎（glob/regex/substring）| 19 测试 |
| P1 | `tool/*` | `WithWorkspace()` 绑定 + 自动注入 + `SubagentTool` | 7+ 测试 |
| P1 | `formatter/` | `ThinkingFormatter` + `extractThinkingBlocks` + 5 后端 | 8+ 测试 |
| P1 | `toolkit/mcp` | MCP Server 适配器 + `fullSession` + `reset_equipped_tools` | 已有测试 |
| P1 | `state/redis_store` | Redis 后端 + TTL + SCAN + prefix | 已有测试 |
| P1 | `service/cipher` | AES-256-GCM + `NewCipherFromEnv()` | 已有测试 |
| P2 | `service/` | `Storage` 接口 + `MemoryStorage` + `RedisStorage` + Auth + CRUD + AES-GCM | 17+ 测试 |
| P2 | `schedule/` | Cron 调度器 `Schedule/Cancel/NextRun` + 重复 ID 替换修复 | 8+ 测试 |
| P2 | `a2a/` | 动态发现 `Registry` + V2 适配器 + 健康检查 | 8+ 测试 |
| P2 | `gateway/` | V2 SSE `/v2/chat/stream` + WebSocket `/v2/chat/ws` + 认证 + Session State Persistence | 5+ 测试 |
| P2 | `service/auth` | API Key + JWT 多租户认证中间件 | 5 测试 |
| P2 | `event/metrics` | 事件流 MetricsCollector + BusWithMetrics + HTTP endpoint | 2+ 测试 |
| P2 | `gateway/auth` | `/api/v1/auth/register` + `/login` + `/me` | 7 测试 |
| P2 | `observability/otel` | `InitTracerProvider` + `OtelTracer` + Gateway HTTP 追踪 | 4+ 测试 |
| P2 | `observability/langsmith` | `LangSmithClient` + `LangSmithObserver` + event.Bus 订阅 | 4 测试 |
| P2 | `gateway/service` | `/api/v1/agents` + `/sessions` + `/credentials` CRUD + 隔离 | 5+ 测试 |
| P2 | `gateway/session_state` | `SaveSnapshot` + `LoadSnapshot` + `Resume` + `DeleteSnapshot` | 11 测试 |
| P1 | `middleware/` | Agent 级五钩子 + `HookOnError` + `LoggingMiddleware` | 6+ 测试 |
| P1 | `toolkit/middleware` | 工具执行层 Logging + Metrics + Permission + Tracing + Offload | 10 测试 |
| P1 | `message/block` | `HintBlock` + JSON 序列化 + `GetHintContent` | 已有测试 |
| P1 | `event/block` | `HintBlockStart/Delta/End` 事件类型 | 已有测试 |
| P3 | `model/router` | 重试/回退/退避路由 | 8 测试 |
| P3 | `model/audio` | `AudioModel` 接口 + `MockAudioModel` + `OpenAITTS` | 6 测试 |
| P3 | `model/deepseek` | DeepSeek HTTP client + SSE stream | 已有测试 |
| P3 | `model/vllm` | vLLM HTTP client + SSE stream | 已有测试 |
| P3 | `model/moonshot` | Moonshot (Kimi) HTTP client + SSE stream | 已有测试 |
| P3 | `model/xai` | xAI (Grok) HTTP client + SSE stream | 已有测试 |
| P2 | `pipeline/parallel` | 并发 Agent 执行 + 自定义聚合 | 7 测试 |
| P2 | `async/pool` | 固定 goroutine 池 + 任务状态跟踪 + 优雅关闭 | 6 测试 |
| P2 | `loader/` | `Loader` interface + `TextLoader` + `DirLoader` | 4 测试 |
| P2 | `tool/subagent` | `SubagentTool` 递归调用 + 嵌套深度限制 | 已有测试 |
| P2 | `message/json` | `source` 嵌套兼容 + PyV2 格式对齐 + `output` 字段 | 已有测试 |
| P2 | `model/openai` | `ResponseFormat` + `json_object` / `json_schema` | 已有测试 |
| P2 | `examples/multi_tenant_workspace` | 端到端多租户 + Workspace + 权限引擎 + Gateway 示例 | 编译通过 |
| P2 | `examples/langsmith` | LangSmith 可观测性集成示例 | 编译通过 |
| P3 | `tests/cross_lang` | Go ↔ Python 双向 JSON 解析验证 | 通过 |
| P3 | `agent/react/reply_stream_bench_test.go` | Benchmark | 纯文本 ~965μs/op；工具调用 ~156μs/op |
| P0 | `message.Msg.AppendEvent()` + 自动持久化 | Event 驱动 Msg 组装 + SessionManager 自动 Upsert | 14 个测试通过 |
| P0 | `message/json.go` | `FinishedAt` + `Usage` JSON round-trip | 已有测试 |
| P0 | `event/lifecycle_events.go` | `ModelCallEndEvent.InputTokens/OutputTokens` | 已有测试 |
| P2 | `service/msg_convert.go` | `MsgToStored` / `StoredToMsg` 完整 content blocks 转换 | 8 个测试通过 |
| P2 | `service/storage.go` | `GetMessage` + `UpsertMessage` 接口扩展 | Memory + Redis 双实现 |
| P2 | `gateway/session_manager.go` | `WithStorage` 自动 Msg 持久化（输入 + 回复 Upsert）| 2 个测试通过 |
| P1 | `tool/file/edit.go` | 文本编辑工具（精确替换 + replace_all）| 7 个测试通过 |
| P1 | `tool/file/glob.go` | 文件路径匹配工具（`**` 递归 + 按时间排序）| 5 个测试通过 |
| P1 | `tool/file/grep.go` | 文本搜索工具（regex + context + 分页）| 10 个测试通过 |
| P2 | `model/openai_response` | OpenAI Response API + SSE 流式解析 | 6 个测试通过 |

### 🚧 剩余骨架与下一步任务（第 5 版更新）

| 优先级 | 任务 | 状态 | 阻塞/说明 |
|--------|------|------|-----------|
| **P2** | `gateway/agui.go` AG-UI Protocol 中间件 | ✅ **已实现** | `?protocol=agui` / `X-Protocol: agui` |
| **P2** | `tool/task/` Task 内置工具（Create/Get/List/Update）| ✅ **已实现** | `state/task.go` + `tool/task/` |
| **P2** | `tool/schedule/` Schedule Agent 工具（Create/List/Stop/View）| ✅ **已实现** | `tool/schedule/` + `gateway/schedule_tools.go` |
| **P2** | `tool/skillviewer.go` SkillViewer 内置工具 | ✅ **已实现** | `skill/skillviewer.go` |
| **P2** | `workspace/mcp_gateway.go` MCP Gateway HTTP 代理 | ✅ **已实现** | 基础 HTTP 端点 |
| **P2** | `workspace/offloader.go` Offloader 正式协议 | ✅ **已实现** | `Offloader` + toolkit 对接 |
| **P2** | `permission/bash_parser.go` Bash AST 解析 | ✅ **已实现** | 复合命令拆分 |
| **P2** | `tool/file/cache.go` 文件读 LRU 缓存 | ✅ **已实现** | Read 工具集成 |
| **P2** | `agent/react/` 外部工具执行接线 | ✅ **已实现** | `ExternalChecker` 接口 |
| **P2** | `gateway/tool_offload.go` Tool Offload 中间件 | ✅ **已实现** | `ToolOffloadMiddleware` + `ToolOffloadManager` |
| **P2** | `gateway/background_task.go` 任务状态机扩展 | ✅ **已实现** | 重试/超时/LastRun/LastError + `UpdateJobMeta` |
| **P2** | `formatter/` MultiAgent + DeepSeek/Moonshot/xAI/Response | ✅ **已实现** | `formatter/providers.go` |
| **P2** | `examples/multi_tenant_workspace/` 运行验证 | ✅ **已验证** | `go test ./examples/multi_tenant_workspace/` + DashScope 实机联调 |
| **P3** | `model/model_card.go` ModelCard / YAML 支持 | ✅ **已实现** | YAML 单文件/目录加载 |
| **P3** | `examples/web_ui/` Web UI 参考实现 | ✅ **已实现** | 轻量 HTML/JS + demo agent；`go run ./examples/web_ui/` |
| **P3** | `workspace/docker.go` Dockerfile 生成 + 健康检查 | ✅ **已实现** | `RenderDockerfile` / `ComputeImageTag` / `HealthCheck` / `BuildImage` |
| ~~P2~~ | ~~`workspace/e2b.go` 文件/命令操作~~ | ✅ **已完成** | envd Connect-RPC 完整实现，25 测试通过 |
| **P3** | `workspace/gateway_mcp_client.go` GatewayMCPClient/Tool | ✅ **已实现** | 主机侧 HTTP 代理 MCP 工具 |
| **P3** | `workspace/e2b_mcp.go` E2B MCP sidecar bootstrap | ✅ **已实现** | `StartE2BMCPGateway` 主机侧网关 |
| **P3** | `gateway/model_handlers.go` ModelCard HTTP API | ✅ **已实现** | `GET /api/v1/models` + `/{id}` |
| **P3** | `gateway/offload_hints.go` Tool Offload hint 注入 | ✅ **已实现** | SSE/WS 推理前 `PopResults` 注入 |
| **P3** | `gateway/task_stop_tool.go` TaskStop 工具 | ✅ **已实现** | 取消后台 offload 任务 |
| **P3** | `runcontext/session.go` 会话上下文 | ✅ **已实现** | SessionManager → Toolkit 中间件共享 session_id |
| **P3** | `examples/web_ui/` Tool Offload + SessionManager | ✅ **已实现** | 共享 slowtool + hint 注入 e2e |
| **P3** | `permission/bash_split_treesitter.go` tree-sitter 可选后端 | ✅ **已实现** | `-tags treesitter` + CGO；默认启发式 |

---

## 14. 附录

### A. PyV2 核心事件类型对照表

| PyV2 事件 | Go V2 事件 | 说明 |
|-----------|-----------|------|
| `ReplyStartEvent` | `ReplyStartEvent` | reply 开始 |
| `TextBlockStartEvent` | `TextBlockStartEvent` | 文本块开始 |
| `TextBlockDeltaEvent` | `TextBlockDeltaEvent` | 文本增量 |
| `TextBlockEndEvent` | `TextBlockEndEvent` | 文本块结束 |
| `ThinkingBlockStartEvent` | `ThinkingBlockStartEvent` | 思考块开始 |
| `ThinkingBlockDeltaEvent` | `ThinkingBlockDeltaEvent` | 思考增量 |
| `ThinkingBlockEndEvent` | `ThinkingBlockEndEvent` | 思考块结束 |
| `ToolCallStartEvent` | `ToolCallStartEvent` | 工具调用开始 |
| `ToolCallDeltaEvent` | `ToolCallDeltaEvent` | 工具参数增量 |
| `ToolCallEndEvent` | `ToolCallEndEvent` | 工具调用结束 |
| `RequireUserConfirmEvent` | `RequireUserConfirmEvent` | 请求用户确认 |
| `RequireExternalExecutionEvent` | `RequireExternalExecutionEvent` | 请求外部执行 |
| `ToolResultStartEvent` | `ToolResultStartEvent` | 工具结果开始 |
| `ToolResultTextDeltaEvent` | `ToolResultTextDeltaEvent` | 工具结果文本增量 |
| `ToolResultEndEvent` | `ToolResultEndEvent` | 工具结果结束 |
| `ReplyEndEvent` | `ReplyEndEvent` | reply 结束 |
| `UserConfirmResultEvent` | `UserConfirmResultEvent` | 用户确认结果（外部注入）|
| `ExternalExecutionResultEvent` | `ExternalExecutionResultEvent` | 外部执行结果（外部注入）|

### B. 术语对照

| Python v2 | Go V2 | 说明 |
|-----------|-------|------|
| `reply_stream()` | `ReplyStream()` | 真事件流 |
| `AgentState` | `state.AgentState` | 可序列化状态 |
| `PermissionEngine` | `permission.Engine` | 权限引擎 |
| `Workspace` | `workspace.Workspace` | 执行环境抽象 |
| `MiddlewareBase` | `middleware.*Interceptor` + `middleware.Chain` | Agent 生命周期洋葱中间件 |
| `toolkit.Middleware` | `toolkit.Middleware` | 仅工具 `ExecuteTool` 执行层 |
| `pre_reply / post_reply / pre_model / post_model` | `hook.HookPreReply / HookPostReply / HookBeforeModel / HookAfterModel` | 经典 Hook（与 Middleware 并存） |
| `on_error` | `hook.HookOnError` + `Base.FireOnError()` | 错误钩子（PyV2 无同名 Middleware） |
| `Toolkit` | `toolkit.Toolkit` | 工具集 |
| `StorageBase` | `service.Storage` / `state.Store` | 持久化存储抽象 |
| `Credential` | `service.Credential` + `service.Cipher` | 凭证管理 + AES-GCM 加密 |
| `app/` | `service/` + `gateway/` + `schedule/` | 服务层（Go 版拆分架构）|
| `async pool` | `async.Pool` | 异步任务池（Go 版独有）|
| `Pipeline` | `pipeline.Parallel` | 并发 Agent 执行（Go 版独有）|
| `Loader` | `loader.Loader` | 文档加载器（Go 版独有）|
| `AG-UI Protocol` | ✅ `gateway/agui.go` | 前端协议中间件 |
| `MCP Gateway` | ✅ `workspace/mcp_gateway.go` | 沙箱内 MCP HTTP 代理 |
| `GatewayMCPClient` | ✅ `workspace/gateway_mcp_client.go` | 主机侧 MCP HTTP 客户端 |
| `Offloader` | ✅ `workspace/offloader.go` | 大上下文外存卸载协议 |
| `ModelCard` | ✅ `model/model_card.go` + `/api/v1/models` | YAML 声明式模型配置 |
| `Task Tools` | ✅ `tool/task/` | 任务管理工具 |
| `TaskStop` | ✅ `gateway/task_stop_tool.go` | 取消后台 offload 任务 |
| `Schedule Tools` | ✅ `tool/schedule/` | Agent 可调用 Cron 调度工具 |
| `SkillViewer` | ✅ `skill/skillviewer.go` | Skill 查看工具 |
| `SubagentTool` | ✅ Go 独有 | PyV2 无 subagent 抽象 |
| `A2A Protocol` | ✅ Go 独有 | PyV2 roadmap 未实现 |
| `Bash AST Parser` | ✅ 启发式 + `-tags treesitter` | PyV2 tree-sitter-bash（smacker/go-tree-sitter） |
| `File Read Cache` | ✅ `tool/file/cache.go` | PyV2 ToolContext LRU |

### C. 模块代码量统计（Go 版，截至 commit `3bf5cb1`）

| 模块 | 代码行数（非测试）| 测试文件数 | 评估 |
|------|------------------|-----------|------|
| `memory/` | ~6,025 | 37 | 🏆 极度领先（PyV2 无独立 memory 包） |
| `tool/` | ~3,021 | 16 | ✅ 完整（含 Task/Schedule/SkillViewer） |
| `model/` | ~2,294 | 17 | ✅ 10 后端 + ModelCard YAML |
| `agent/` (含 react) | ~2,564 + ~1,984 | 12 + 9 | ✅ 完整 |
| `middleware/` | ~250 | 2 | ✅ Agent 级 MiddlewareBase 对齐 |
| `gateway/` | ~1,986 | 13 | ✅ 较完整（含 AG-UI/Tool Offload/Model API） |
| `toolkit/` | ~1,412 | 12 | ✅ 较完整（含 MCP Gateway client） |
| `event/` | ~1,004 | 4 | ✅ 22 事件类型对齐 |
| `service/` | ~1,047 | 5 | ✅ 较完整 |
| `workspace/` | ~908 | 4 | ✅ E2B 完整；Docker 基础；缺 MCP Gateway |
| `a2a/` | ~826 | 6 | 🏆 Go 独有 |
| `skill/` | ~849 | 2 | ⚠️ 有 Box + load tool，缺 SkillViewer |
| `permission/` | ~698 | 2 | ⚠️ 规则引擎完整，Bash 解析启发式 |
| `formatter/` | ~652 | 5 | ⚠️ 5/9 后端，无 MultiAgent 变体 |
| `observability/` | ~404 | 4 | ✅ OTel + LangSmith |
| `async/` | ~169 | 1 | ✅ 任务池（未暴露为 Agent 工具） |
| `schedule/` | ~89 | 1 | ⚠️ Cron 引擎有，Agent 工具无 |
| `loader/` | ~111 | 1 | ✅ Go 独有 |
| `pipeline/` | ~130 | 3 | ✅ Go 独有 |
| `docs/` | ~1,200 (md) | 0 | ✅ 骨架 |
| **Go 总计** | **~29,454** | **174** | - |

**PyV2 对照（`src/agentscope/`）**：

| 模块 | 行数 | 说明 |
|------|------|------|
| `app/` | 6,467 | 服务层（Go 拆分为 gateway + service + schedule） |
| `tool/` | 5,294 | 含 Task 工具 + Bash parser |
| `model/` | 4,513 | 含 33 YAML ModelCard |
| `workspace/` | 4,340 | 含 MCP Gateway + Docker 深度 |
| `formatter/` | 3,662 | 9 后端 × MultiAgent |
| `agent/` | 2,268 | 含 context/tool compression |
| `middleware/` | 1,316 | OTel tracing |
| `embedding/` | 955 | 含 FileEmbeddingCache |
| **PyV2 总计** | **~31,902** | 54 测试文件 / ~619 测试函数 |

### D. 模型后端对照表（修正版）

| 后端 | PyV2 | Go 版 | 说明 |
|------|------|-------|------|
| OpenAI Chat | ✅ | ✅ | Chat Completion API |
| OpenAI Response | ✅ | ✅ | Response API（reasoning 流式事件） |
| Anthropic | ✅ | ✅ | Claude API |
| Gemini | ✅ | ✅ | Google API |
| DashScope | ✅ | ✅ | 阿里云 |
| Ollama | ✅ | ✅ | 本地部署 |
| DeepSeek | ✅ | ✅ | 深度求索 |
| Moonshot | ✅ | ✅ | 月之暗面（Kimi） |
| xAI | ✅ | ✅ | Grok |
| vLLM | ❌ | ✅ | **Go 版独有**（私有化部署）|
| **总计** | **9** | **10** | Go 版多 1 个（vLLM） |

### E. AG-UI Protocol 核心映射（参考 PyV2）

```python
# PyV2 _agui.py 核心映射逻辑（简化）
AGUI_EVENT_MAP = {
    "TextBlockStartEvent":    "AGUIMessageStartEvent",
    "TextBlockDeltaEvent":    "AGUIMessageContentEvent",
    "TextBlockEndEvent":      "AGUIMessageEndEvent",
    "ThinkingBlockStartEvent": "AGUIReasoningMessageStartEvent",
    "ThinkingBlockDeltaEvent": "AGUIReasoningMessageContentEvent",
    "ThinkingBlockEndEvent":  "AGUIReasoningMessageEndEvent",
    "ToolCallStartEvent":     "AGUIToolCallStartEvent",
    "ToolCallEndEvent":       "AGUIToolCallEndEvent",
    "ToolResultStartEvent":   "AGUIToolResultStartEvent",
    "ToolResultEndEvent":     "AGUIToolResultEndEvent",
    "ReplyStartEvent":        "AGUIReplyStartEvent",
    "ReplyEndEvent":          "AGUIReplyEndEvent",
}
```

Go 版实现时需将上述映射翻译为 `gateway/agui.go` 中的 `switch` 语句或 map 查找。

### F. Python v2 与 Go 版 Workspace 代码量对比

| Workspace 类型 | Python v2 代码量 | Go 版代码量 | 差距倍数 |
|----------------|------------------|-------------|----------|
| Docker | 1,229 行 | ~215 行 | ~5.7× |
| E2B | 1,034 行 + MCP Gateway | ~519 行（**FS/命令 ✅**）| ~2.0×（核心能力已追平） |
| Local | ~300 行（估算）| 142 行 | ~2.1× |

---

> **结语**：AgentScope.Go V2 的核心不是追逐功能数量，而是完成**架构范式的对齐**，并在 Memory、A2A、SubagentTool 等领域形成差异化领先。P0 事件驱动范式、P1 生产级能力与 P2 服务化已基本闭环；**第 5 版对比修正了 E2B 实现状态（已由 stub 升级为完整 FS/命令实现）**，并新识别 MCP Gateway、Schedule Agent 工具、Bash AST 解析器、文件读缓存等差距。Go 版与 PyV2 形成**互补而非替代**的定位——PyV2 是 Python 生态的完整平台（含 web_ui + AG-UI + MCP Gateway），Go V2 是高性能、强类型、云原生部署的首选方案。
>
> **当前最紧迫的七项任务**：
> 1. `gateway/agui.go` AG-UI Protocol 中间件（Studio UI 即插即用）；
> 2. `tool/task/` Task 内置工具 + `tool/schedule/` Schedule Agent 工具；
> 3. `tool/skillviewer.go` SkillViewer 内置工具；
> 4. `workspace/mcp_gateway.go` MCP Gateway HTTP 代理；
> 5. `workspace/offloader.go` Offloader 正式协议；
> 6. `permission/bash_parser.go` Bash AST 解析器（或管道拆分预处理）；
> 7. `workspace/docker.go` Dockerfile 生成 + 容器生命周期管理。
