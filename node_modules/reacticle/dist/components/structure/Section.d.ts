import { type ReactNode } from "react";
import "./structure.css";
export interface SectionProps {
    /** Section heading. Required. */
    title?: string;
    /** Optional ordinal shown before the title, e.g. "01". */
    index?: string;
    /** Optional anchor id for navigation. */
    id?: string;
    children: ReactNode;
}
/** A first-level section of the article. */
export declare function Section({ title, index, id, children }: SectionProps): import("react").JSX.Element;
//# sourceMappingURL=Section.d.ts.map