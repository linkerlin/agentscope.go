# AgentScope (Python) vs AgentScope.Go 深度对比与演进方案 v2

> **版本**：2026-07-03 深度修订版（取代 2026-06-12 旧版）
> **基准快照**：
> - Python 版：`C:/GitHub/agentscope/` main 分支（2026-07-03 工作区），AgentScope 2.0
> - Go 版：`C:/GitHub/agentscope.go/` v2.3.0（2026-06-27 工作区）
>
> **修订背景**：旧版方案（2026-06-12）基于彼时代码快照，将 RAG/服务化等多项判定为"已对齐"。但 **Python 版自 2026-06 起经历了一轮服务化跃迁**（知识库托管服务、生产级消息总线、React Web UI、Agent 团队异步协作运行时），拉开了 Go 版此前未充分评估的差距。本版基于 **2026-07-03 双方真实源码**重新深度审阅，聚焦 Python 真实领先的维度，制定精准的 Go 演进路线。

---

## 0. 核心结论（先读这段）

1. **Go 版在"分布式 + 性能 + 前沿特性"上仍然领先**：A2A 协议、ReMe 记忆、GEP 自演化、ONNX 推理代理、Plugin 系统、SQLiteVec、断路器、DAG 执行器、Streamable HTTP —— 这些是 Go 独有或反超的能力，**必须继续放大**。
2. **Python 版在"托管服务化"上拉开了新的、此前被低估的差距**，集中在三个此前方案漏判或低估的领域：
   - **RAG 托管知识库服务**（解析→分块→存储→索引工作流→检索中间件）：Go 几乎空白。
   - **生产级消息总线 + 服务编排层**（`app/` 工厂 + ChatService/SessionProjection/IndexWorker/BackgroundTask/Wakeup/Cancel 全套调度器 + 领域中间件）：Go 的 `messagebus`/`gateway` 偏薄。
   - **React 单页 Web UI**：Go 仅有静态 HTML + 服务端渲染 Studio。
3. **本方案不再追求"逐行复刻 Python"**，而是以 Go 地道方式（单二进制、`embed`、channel、interface 组合）补齐"托管服务化"这一能力带，同时守住并放大既有领先项。

---

## 1. 维度化深度对比（2026-07-03 真实源码）

> 下表基于对双方 `src/` 与根目录的逐文件审阅。代码量按非测试/测试行近似估算。

| 维度 | Python 2.0（07-03） | Go v2.3.0 | 领先方 | 真实差距说明 |
|------|---------------------|-----------|--------|--------------|
| **RAG：托管知识库服务** | **完整**：parser(PDF/PPT/Image/Text) + Chunker + VDB(Milvus/Qdrant) + `app/rag`(BlobStore Local/S3 + KBManager + IndexWorker + IndexSweeper + IndexTaskConsumer) + KB HTTP Router + `RAGMiddleware`(29KB) | **薄**：`rag/` 仅 Tika HTTP 适配 + knowledge_tool + memory_rag；无 parser/chunker/blob/index-worker/KB-manager/RAG-middleware | **Python 大幅领先** | Go 缺整条"文档→索引→检索"托管管道与服务层。**最大单点差距。** |
| **Agent 服务编排层 `app/`** | **完整**：`create_app()` 工厂 + 9 个 Router + ChatService/SessionService/KBService/IndexWorker/Toolkit/TTS + BackgroundTaskManager + WakeupDispatcher + CancelDispatcher + ChatRunRegistry + SchedulerManager + 领域中间件(Inbox/StateChange/ToolOffload) | **偏 HTTP 层**：`gateway/` 有 server/handlers/session_manager/team_tools/offload/wakeup，但无 `create_app` 式可嵌入工厂、无统一服务编排抽象 | **Python 领先** | Go 服务化深度不足，缺可插拔 app 工厂与服务编排总线 |
| **消息总线** | **领域无关完整**：`MessageBus` base(28KB) + InMemory(15KB) + Redis(22KB) + Keys(9KB)；publish/subscribe/queue/registry/lock/projection 全套原语 | **薄**：`messagebus` bus.go(4KB)+redis.go(6KB)+team_bus.go(3KB)；仅 team inbox/wakeup | **Python 领先** | Go 缺分布式锁、registry、queue、跨会话投影原语 |
| **Web UI** | **完整 React SPA**：React19+Vite+Tailwind+Radix+i18n，pages/components/hooks/context/api；演示 Team/Plan/Permission/Offload | **静态 HTML**：`examples/web_ui/static` 3 文件 + 服务端渲染 Studio | **Python 大幅领先** | Go 无现代 SPA |
| **记忆系统** | 短期压缩 + `middleware/_longterm_memory`(ReMe/mem0/**AgenticMemory** 新) | ReMe(文件/向量/Hybrid/Dream/KG) + mem0 适配器 | **Go 领先（功能量）**，Python 有 Agentic Memory 新范式 | Go 缺 Agentic Memory（agent 自主管理记忆） |
| **多 Agent 编排** | Msg 列表 + MultiAgentFormatter + Agent Team 异步协作 | Pipeline/MsgHub/Workflow/Reflection/MapReduce + Team 同步/异步双模 | **Go 领先** | — |
| **A2A 协议** | Roadmap 提及，无实现 | 完整(AgentCard/Task/SSE/Registry/ShardRouter/ClusterManager/安全/Redis) | **Go 大幅领先** | — |
| **工具集** | Bash/Read/Write/Edit/Grep/Glob + Task(create/get/list/update) + Schedule + Skill + Team | file/shell/web/json/multimodal + Task/Schedule/Subagent/Skill + Team + A2A | **基本持平**，Go 多 Subagent/A2A；Python Task 工具更完整(update 9.5KB) | Go Task 工具 CRUD 可补全 |
| **模型后端** | 9 家 + Ollama | 10 家(含 vLLM) + Responses API + Router + 断路器 + ModelCard | **Go 略领先** | — |
| **嵌入模型** | 6 类(含 DashScope 多模态) | 5 类 + FileCache + ModelCard + ONNX 代理 | **基本持平** | — |
| **Workspace 沙箱** | Local/Docker/E2B + MCP Gateway + `app/workspace_manager` 服务层 | Local/Docker/E2B + MCP Gateway + Offloader | **基本持平** | Python 多服务层 workspace_manager |
| **权限/HITL** | PermissionEngine + 5 模式(EXPLORE 等) + 5 种 PermissionMode | PermissionEngine + Bash AST + 4 模式 | **基本持平** | Go 可补 EXPLORE 等预设模式 |
| **可观测性/Tracing** | OTel + `middleware/_tracing` extractor(20KB)+trace(12KB)+attributes(7KB) | OTel + LangSmith + TracingMiddlewareAdapter + otelbridge | **Python 略领先** | Python span 提取更丰富 |
| **存储层** | `redis_storage`(64KB) + base(29KB) 全实体 CRUD + 投影 + 索引态 | `redis_storage`(20KB) + memory_storage(10KB) + entities | **Python 领先（深度）** | Go 覆盖实体但深度/投影不足 |
| **自演化/GEP** | Roadmap | evolver/(Gene/Capsule/Evolver 16 方法/Skill2GEP/MCP) | **Go 领先** | — |
| **Plugin 系统** | 无 | plugin/(接口/Manager/Registrar/YAML/.so) | **Go 领先** | — |
| **实时语音** | Roadmap | AudioModel + TTS 独立包 + Voice 示例 + VAD + 打断 | **Go 领先** | — |
| **文档/社区/分发** | docs.agentscope.io + PyPI + ~50 贡献者 + 论文 | MkDocs 站点 + go get + 3 贡献者 | **Python 大幅领先** | 见旧版方案，本版不重复 |

---

## 2. Python 领先点深度剖析（按严重度分级）

### 🔴 TIER 1：关键能力差距（直接影响生产可用性与开发者采用）

#### 2.1 RAG 托管知识库服务（最大差距）

**Python 现状**（一条完整的"文档→检索"托管管道）：

| 子系统 | Python 实现 | 代码量 |
|--------|------------|--------|
| **文档解析 Parser** | `rag/_parser/`：PDF(`pdfminer`)、PPT(`python-pptx`，逐页+表格+图片)、Image(OCR)、Text、base 抽象 | base 4.8KB + ppt 12.5KB + pdf 3KB + image 2.7KB + text 4KB |
| **分块 Chunker** | `rag/_chunker/_approx_token_chunker.py`：近似 token 切分（UTF-8 字节/4，无 tokenizer 依赖），可配 chunk_size/overlap，DataBlock 原样透传 | 5.8KB |
| **文档模型** | `rag/_document.py`：Document/Section/Chunk 数据模型 + 元数据(slide_index 等) | 3.9KB |
| **向量库 VDB** | `rag/_vdb/`：Milvus Lite、Qdrant、VectorStoreBase 抽象 + VectorRecord/SearchResult/DocumentSummary | base 10KB + milvus 15KB + qdrant 13KB |
| **运行时句柄** | `rag/_knowledge.py`：`KnowledgeBase` 类 —— 绑定 embedding+collection，暴露 search/insert/delete/list，懒建表，`metadata_filter` 多租户隔离 | 15KB |
| **Blob 存储** | `app/rag/blob_store/`：Local + **S3**，存储上传文档字节，AsyncReadable | base 5.7KB + local 5.2KB + s3 11.6KB |
| **知识库管理器** | `app/rag/knowledge_base_manager/`：CollectionPerKb(一 KB 一 collection) + DimensionPolicy(维度策略校验) + 生命周期管理 | base 10KB + collection_per_kb 6.4KB + dimension 5.7KB |
| **索引工作流** | `app/_service/`：IndexWorker(20KB，parser 注册表+pipeline+心跳+错误标记) + IndexTaskConsumer(8KB) + IndexSweeper(5.6KB，失败重试) | 33KB |
| **HTTP 路由** | `app/_router/_knowledge_base.py`：KB CRUD + 文档上传 + 索引状态查询 | 20KB |
| **RAG 中间件** | `middleware/_rag.py`：`RAGMiddleware` 自动注入 `search_knowledge` 工具、on_reasoning 检索、HintBlock 注入、跨 KB 搜索 | **29KB** |

**Go 现状**：`rag/` 仅 `rag.go`(2KB) + `knowledge_tool.go`(2KB) + `tika.go`(2KB 外部 Tika HTTP) + `memory_rag.go`(2KB) + `rag_hook.go`(2KB) + `document.go`(**155 字节**桩)。
**差距结论**：Go 缺整条管道的 90%。这是"Agent 能不能直接吃 PDF/PPT 建知识库"的核心能力。

#### 2.2 生产级消息总线（跨进程协调的骨干）

**Python 现状**：`app/message_bus/` 提供**领域无关**的完整原语集，是团队协作、工具卸载、唤醒、跨会话投影的统一骨干：
- `MessageBus` base(28KB)：`publish/subscribe`（pub/sub）+ `queue_push/queue_pop`（队列）+ `registry_set/get/list`（注册表）+ `acquire_lock`（分布式锁）+ `log_append/log_read`（日志）
- `MessageBusKeys`(9KB)：业务键约定中心（wakeup kind: wake/resume、session projection namespace）
- 后端：InMemory(15KB) + Redis(22KB pub/sub)
- 之上构建：InboxMiddleware（团队消息排空）、WakeupDispatcher（空闲会话唤醒）、CancelDispatcher（取消分发）、ToolOffloadMiddleware（卸载结果回投）、session_projection（跨会话 UI 卡片投影）

**Go 现状**：`messagebus/` bus.go(4KB)+redis.go(6KB)+team_bus.go(3KB)，仅 team inbox(`InboxPush/Drain`)+wakeup(`EnqueueWakeup/SubscribeWakeup`)。
**差距结论**：Go 缺分布式锁、registry、通用 queue、log 原语。这些是"多进程安全协调"的基础设施。

#### 2.3 React 单页 Web UI

**Python 现状**：`examples/web_ui/` 是 pnpm monorepo：
- `frontend/`：React 19 + Vite + TypeScript + Tailwind + Radix UI + i18next；`src/` 含 api/components/context/hooks/i18n/lib/pages/types
- `backend/`：TypeScript 代理层
- 演示能力（有 GIF）：Agent Team 协调、任务规划、权限 bypass 模式、后台任务卸载唤醒
- 仓库自带 `pnpm-lock.yaml`(341KB)，是真实可部署工程

**Go 现状**：`examples/web_ui/static/`（app.js + index.html + styles.css，3 文件）+ 服务端渲染 Studio（`studio/` html/template）。
**差距结论**：Go 无现代 SPA，首屏体验与交互丰富度差距大。

---

### 🟠 TIER 2：重要能力差距（影响服务化深度与可观测性）

#### 2.4 Agent 服务编排层 `app/`（可嵌入工厂 + 服务编排总线）

**Python 现状**：`create_app(storage, message_bus, workspace_manager, knowledge_base_manager, ...)` 是**可嵌入任意 FastAPI**的工厂，支持：
- 可插拔：storage / message_bus / workspace_manager / KB manager / blob_store / parsers / chunker / extra_middlewares / extra_agent_tools / custom_subagent_templates / custom_agent_cls
- 嵌入式 vs 独立式：可 `root.mount("/agentscope", app)` 挂到已有应用
- 服务编排：ChatService.run() → `_project_event` 事件投影；SessionService 含 session_projection
- 三层 Manager：BackgroundTaskManager(15KB) + WakeupDispatcher(14KB) + CancelDispatcher(6.5KB) + ChatRunRegistry + SchedulerManager(16KB)

**Go 现状**：`gateway/server.go`(13KB) + `app.go`(7.6KB) 偏"HTTP 服务器"，缺统一可插拔工厂与服务编排抽象。
**差距结论**：Go 服务化是"能跑的 HTTP"，不是"可编排的服务平台"。

#### 2.5 Tracing 提取丰富度

**Python 现状**：`middleware/_tracing/extractor.py`(20KB) 从 Agent 事件提取丰富 OTel span 属性 + `attributes.py`(7KB) + `trace.py`(12KB) + `converter.py`。
**Go 现状**：`observability/`(477 行) OTel+LangSmith+otelbridge，span 提取较简。
**差距结论**：Go tracing 可观测的"语义粒度"不足。

#### 2.6 存储层深度

**Python 现状**：`app/storage/_redis_storage.py`(64KB) + `_base.py`(29KB) —— 全实体 CRUD + 投影 + 索引态 + 调度态。
**Go 现状**：`service/redis_storage.go`(20KB) + memory_storage(10KB)。
**差距结论**：Go 实体覆盖到位但投影/索引态/复杂查询深度不足。

#### 2.7 Agentic Memory（Python 新范式）

**Python 现状**：`middleware/_longterm_memory/_agentic_memory/` —— agent **自主决策**记什么/忘什么的长期记忆（区别于被动检索的 ReMe）。
**Go 现状**：ReMe(被动检索) + mem0 适配器，无"agent 自主管理记忆"范式。
**差距结论**：Go 缺最新记忆范式。

---

### 🟡 TIER 3：中度差距（体验与完整度）

#### 2.8 Task 工具 CRUD 完整度
Python `tool/_task/`：create(4.6KB)+get(3KB)+list(2.7KB)+update(**9.5KB**，含状态机/依赖/依赖结果传递)。Go Task 工具 CRUD 较简。

#### 2.9 预设权限模式
Python `PermissionMode`：EXPLORE 等预设只读/受限模式，可直接用于 SubAgentTemplate。Go 需手动配规则。

#### 2.10 Workspace 服务层管理器
Python `app/workspace_manager/`：服务层 workspace 生命周期 + 默认 MCP 注入。Go workspace/ 是工具层，gateway/workspace_manager.go(10KB) 偏 HTTP。

#### 2.11 SubAgentTemplate 声明式模板
Python `SubAgentTemplate`：声明式定义子 agent 类型(type/system_prompt_template/permission_context)，leader 通过 `AgentCreate(subagent_type=...)` 路由。Go 有 `SubagentTemplates` 但声明式模板成熟度略低。

---

## 3. Go 版差异化优势（须继续放大，勿在补齐中稀释）

| 优势 | 现状 | 放大方向 |
|------|------|----------|
| **A2A 分布式协议** | ShardRouter/ClusterManager/Redis 注册/安全/故障转移 watch | 打造成"多语言 Agent 网格"事实标准 |
| **ReMe 记忆** | 文件/向量/Hybrid/Dream/KG/SQLiteVec | 结合 Agentic Memory 形成"被动+主动"双模 |
| **GEP 自演化** | evolver/ Gene/Capsule/Skill2GEP/MCP | 闭环落地为"自愈 Agent"卖点 |
| **Plugin 系统** | .so 动态加载 + YAML 配置 | 生态插件市场雏形 |
| **ONNX 推理代理** | CLIP/Whisper 预处理 + HTTP 代理 | 本地多模态零 CGO |
| **性能工程** | pprof + benchmark Catalog + Makefile 对比 | 发布权威性能报告 |
| **断路器/DAG/Streamable HTTP** | 三状态断路器 + Kahn DAG + MCP 流式 HTTP | 生产稳定性卖点 |
| **TTS 独立包** | DashScope/OpenAI + RealtimeModel | 实时语音领先 |

---

## 4. 演进总体战略

> **愿景**：让 AgentScope.Go 成为"**云原生、高并发、长记忆、自演化、托管式** Agent 服务的首选 Go 框架"。
> 补齐"托管服务化"能力带（RAG KB / 消息总线 / 服务编排 / 现代 UI），同时守住分布式与前沿特性领先。

### 4.1 战略原则

1. **补齐不复制**：每个 Python 能力用 Go 地道方式实现——单二进制、`embed.FS` 内嵌资源、interface 组合、channel 背压，拒绝逐行翻译。
2. **能力带优先**：本轮聚焦 TIER 1（RAG KB / 消息总线 / UI），它们是"生产可用"的门槛，不是锦上添花。
3. **守住护城河**：A2A/ReMe/evolver/Plugin 是 Go 独有，每个新阶段都要有"放大既有优势"的任务，禁止只补不攻。
4. **最小依赖**：parser 优先纯 Go 库（`unioffice`/PPT、`pdf`/PDF、`golang.org/x/image`），blob 首版只做 Local+S3，避免一上来铺全家桶。
5. **可验证**：每阶段交付 = 可运行示例 + 通过的测试（`go test ./... -race`）+ 文档。

### 4.2 演进路线总图（修订）

```
2026-Q3  现状：分布式/前沿领先，托管服务化滞后
    │
    ▼
Phase 5（本轮 P0）：RAG 托管知识库服务  ← 最大差距，最先补
    │   parser/chunker/document/BlobStore/KBManager/IndexWorker/RAGMiddleware/KB Router
    ▼
Phase 6（本轮 P1）：消息总线深化 + 服务编排层
    │   分布式锁/registry/queue/log 原语 + create_app 工厂 + ChatService 编排
    ▼
Phase 7（本轮 P2）：现代 Web UI（embed SPA / 兼容 Python Studio）
    │
    ▼
Phase 8（本轮 P3）：记忆范式 + 可观测 + 存储深化 + Task 工具
    │   Agentic Memory / Tracing 提取 / 存储投影 / Task CRUD / 预设权限模式
    │
    （旧 Phase 1-4 中已完成项不再重复；未完成项见 TODO.md）
```

---

## 5. 分阶段演进方案（本轮新增）

### Phase 5：RAG 托管知识库服务（P0，目标 2026-Q3）

> 对标 Python `rag/` + `app/rag/` + `middleware/_rag.py`。用 Go 纯库实现解析/分块，避免重依赖。

#### 5.1 文档模型与解析器 `rag/document/` + `rag/parser/`
- `document.go`：`Document`/`Section`/`Chunk` 数据模型（对齐 Python，含 metadata slide_index/media_type），**替换现有 155 字节桩**。
- `parser/parser.go`：`Parser` 接口 `Parse(ctx, reader, mediaType) ([]Section, error)` + `SupportedMediaTypes()` + 注册表 `Registry`（按 mediaType 路由）。
- `parser/text.go`：TextParser（纯 stdlib）。
- `parser/pdf.go`：PDFParser，基于 `github.com/ledongthuc/pdf` 或 `github.com/dslipak/pdf`（纯 Go，无 CGO）。
- `parser/pptx.go`：PPTXParser，基于 `github.com/unidoc/unioffice` 或 `github.com/pterm/ptunioffice`（逐页 + 表格 + 图片提取）。
- `parser/image.go`：ImageParser（base64 + media_type，OCR 留接口，首版可调外部 OCR/多模态模型）。

#### 5.2 分块器 `rag/chunker/`
- `chunker.go`：`Chunker` 接口 `Chunk([]Section) ([]Chunk, error)`。
- `approx_token.go`：`ApproxTokenChunker`——`len(utf8)/4` 近似 token，chunk_size/overlap 可配，DataBlock 原样透传（对齐 Python，零 tokenizer 依赖）。
- 预留 `recursive.go` 递归分块（Phase 后续）。

#### 5.3 Blob 存储 `rag/blob/`
- `blob.go`：`BlobStore` 接口 `Put(ctx, key, reader) (uri, error)` / `Get(ctx, uri) (io.ReadCloser, error)` / `Delete`。
- `local.go`：`LocalBlobStore`（基于 `os`，根目录可配）。
- `s3.go`：`S3BlobStore`（基于 `github.com/aws/aws-sdk-go-v2`，Phase 5.3 可选，首版 Local 即可验收）。

#### 5.4 知识库管理器 `rag/kb/`
- `kb.go`：`KnowledgeBase` 运行时句柄（绑定 embedding+vectorstore+collection，懒建表，search/insert/delete/list，`metadata_filter` 多租户隔离）。
- `manager.go`：`KBManager` 接口（生命周期 Create/Get/List/Delete + 运行时句柄获取）+ `CollectionPerKB` 实现（一 KB 一 collection，复用现有 `memory/vector` 后端）。
- `dimension.go`：`DimensionPolicy`（embedding 维度一致性校验）。

#### 5.5 索引工作流 `rag/index/`
- `worker.go`：`IndexWorker`——消费索引任务 → 读 blob → parse → chunk → embed → 写 vectorstore，含心跳(`context`)+错误标记。
- `consumer.go`：`IndexTaskConsumer`——从消息总线队列消费任务（依赖 Phase 6 的 queue 原语；Phase 5 可先用 channel 内部队列）。
- `sweeper.go`：`IndexSweeper`——扫描失败任务重试。
- **ponytail**：首版 worker 可单进程内 goroutine + channel，验证管道通后再接消息总线。

#### 5.6 RAG 中间件 `middleware/rag.go`
- `RAGMiddleware`：实现洋葱中间件——
  - `list_tools`：自动注入 `search_knowledge` 工具（按 KB 描述生成 schema）
  - `on_reasoning`：对用户输入跨 KB 检索，结果格式化为 `HintBlock` 注入上下文
  - `on_reply`：可选，记录检索命中
- 复用现有 `message.HintBlock` + `tool.Response` 规范。

#### 5.7 KB HTTP 路由 `gateway/kb_handlers.go`
- `GET/POST /api/v1/knowledge-bases`（CRUD）
- `POST /api/v1/knowledge-bases/{id}/documents`（上传文档 → 入 blob → 派发索引任务）
- `GET /api/v1/knowledge-bases/{id}/documents`（文档列表 + 索引状态）
- `DELETE /api/v1/knowledge-bases/{id}/documents/{doc_id}`
- `POST /api/v1/knowledge-bases/{id}/search`（手动检索）
- `gateway/app.go` 增加 `WithKBManager()` 链式选项。

#### 5.8 示例与测试
- `examples/rag_kb/`：上传 PDF/PPT → 索引 → 提问，端到端 demo。
- `rag/parser/*_test.go`：各 parser 单测（用 `testdata/` 固定样本）。
- `rag/chunker/*_test.go`：token 近似 + overlap + DataBlock 透传。
- `middleware/rag_test.go`：中间件注入与检索注入。
- **ponytail 检查**：`rag/parser/demo()` 或最小 `test_*` 确保管道断言可运行。

**Phase 5 验收**：
- [ ] PDF/PPT/Text/Image 4 parser 可解析真实样本
- [ ] ApproxTokenChunker 切分 + overlap 正确
- [ ] LocalBlobStore + KBManager(CollectionPerKB) CRUD 通过
- [ ] IndexWorker 单进程端到端：上传→索引→可检索
- [ ] RAGMiddleware 注入 search_knowledge 工具 + on_reasoning 检索
- [ ] KB HTTP 路由 CRUD + 上传 + 搜索通过
- [ ] `go test ./... -race` 全绿

---

### Phase 6：消息总线深化 + 服务编排层（P1，目标 2026-Q4）

#### 6.1 消息总线原语补齐 `messagebus/`
对齐 Python `MessageBus` 领域无关原语，在现有 `Bus` 接口上**追加**（不破坏 team_bus）：
- `Lock(ctx, key, ttl) (release func, error)`：分布式锁（Redis `SET NX EX` / Local `sync.Mutex` map）。
- `RegistrySet/Get/List/Delete(ctx, ns, key, val)`：注册表（复用 a2a 已有 Registry 思路，但泛化到 bus 层）。
- `QueuePush/QueuePop(ctx, name, val)`：通用队列（Redis `LPUSH/BRPOP` / Local channel）。
- `LogAppend/LogRead(ctx, ns, val, cursor)`：追加日志。
- `messagebus/keys.go`：业务键约定（wakeup kind: wake/resume、projection namespace）。
- **ponytail**：Local 后端用现成 stdlib；Redis 后端复用 `go-redis`，不引新依赖。

#### 6.2 跨会话投影 `gateway/projection.go`
- `SessionProjection`：一个会话可向另一会话投影 UI 卡片（如 worker 的 HITL 请求投影到 leader），存 bus registry/log。

#### 6.3 服务编排层 `gateway/app.go` 增强
- `CreateApp(opts...)` 工厂函数（对齐 `create_app`）：可插拔 Storage/Bus/WorkspaceManager/KBManager/BlobStore/ExtraMiddlewares/ExtraAgentTools/SubagentTemplates/CustomAgentClass。
- `ChatService` 编排：统一 `Run(ctx, sessionID, input)` → 事件投影 → 唤醒/卸载/取消分发（复用现有 wakeup_dispatcher + background_task + team_tools，提炼为编排层）。
- `gateway/middleware/`：新增领域中间件目录
  - `inbox_middleware.go`：团队消息排空注入（对齐 Python InboxMiddleware）
  - `state_change_middleware.go`：会话状态变更广播
  - `tool_offload_middleware.go`：超时工具卸载（已有 `gateway/tool_offload.go`，提炼为标准中间件接入编排）

**Phase 6 验收**：
- [ ] Bus 分布式锁/registry/queue/log 四原语 Local+Redis 双后端测试通过
- [ ] CreateApp 工厂可嵌入（作为子路由挂载）
- [ ] ChatService 统一编排：卸载→唤醒→团队消息 三路打通
- [ ] InboxMiddleware 排空 + 注入验证
- [ ] `go test ./... -race` 全绿

---

### Phase 7：现代 Web UI（P2，目标 2027-Q1）

#### 7.1 双轨策略（与旧版一致，深化执行）
**方案 A（主，Go 原生）**：embed SPA
- 用 Vite + React/Vue/Svelte 构建 `examples/web_ui/frontend/`，`go:embed dist` 内嵌进单二进制。
- 页面对齐 Python：Agent/会话/凭证/模型/知识库/权限/日程/团队/MCP 管理 + SSE 流式聊天。
- 复用现有 AG-UI 协议，前端直连 gateway。

**方案 B（兼容）**：验证 Python `examples/web_ui` React 前端可直连 Go gateway（AG-UI 协议兼容），文档给出"Python 前端 + Go 后端"部署方案。

#### 7.2 Studio 深化
- 现有服务端 Studio 增加：**知识库管理页**（对应 Phase 5 KB 路由）、**索引状态监控**、**Blob 浏览**。

**Phase 7 验收**：
- [ ] embed SPA 单二进制可启动，核心页面可用
- [ ] SSE 流式聊天端到端
- [ ] Python Studio 前端连 Go gateway 兼容性验证通过
- [ ] 文档：双轨部署方案

---

### Phase 8：记忆范式 / 可观测 / 存储 / 工具补全（P3，目标 2027-Q1）

#### 8.1 Agentic Memory `middleware/agentic_memory.go`
- 对齐 Python `_agentic_memory`：agent 自主决策记忆的存取。实现 `AgenticMemory` 接口（基于现有 ReMe 向量库），中间件暴露 `remember`/`recall`/`forget` 工具给 agent 自主调用，区别于被动检索的 ReMe。
- 与现有 `LongTermMemoryMiddleware`(static_control/agent_control/both) 区分：Agentic 强调"agent 自己决定记什么"。

#### 8.2 Tracing 提取深化 `observability/extractor.go`
- 对齐 Python extractor：从 Agent 事件提取丰富 span 属性（model/tool/usage/token/reasoning step），丰富 otelbridge。

#### 8.3 存储投影与索引态 `service/`
- `redis_storage.go` 增加：知识库索引态、session projection、调度态的读写。

#### 8.4 Task 工具 CRUD 补全 `tool/task/`
- 对齐 Python：TaskUpdate 支持状态机（todo/in_progress/done/blocked）+ 依赖 + 依赖结果传递。

#### 8.5 预设权限模式 `permission/`
- 新增 `PermissionMode` 常量：`EXPLORE`(只读)/`STANDARD`/`AUTONOMOUS` 等，可直接用于 SubAgentTemplate。

**Phase 8 验收**：
- [ ] Agentic Memory 中间件：agent 自主 remember/recall 验证
- [ ] Tracing span 属性粒度对齐 Python
- [ ] TaskUpdate 状态机 + 依赖通过
- [ ] 预设权限模式可一键应用
- [ ] `go test ./... -race` 全绿

---

## 6. 跨阶段工程化与社区（贯穿，承接旧版）

> 旧版 Phase 1-4 的 CI/文档/示例/发布任务多数已完成（见 TODO.md）。本轮聚焦：

- **测试**：每个新增 parser/chunker/blob/kb/index 模块必须带 `test_*`；KB 路由加集成测试（`//go:build integration`）。
- **文档**：`docs/RAG.md`（托管 KB 教程）、`docs/MESSAGE_BUS.md`（总线原语）、`docs/WEB_UI.md`（双轨部署）。
- **示例**：`examples/rag_kb/`、`examples/agentic_memory/`。
- **性能**：KB 索引吞吐 + 检索延迟纳入 `benchmark/` Catalog。
- **契约**：保持 RAG document/chunk JSON 与 Python 兼容（`tests/cross_lang`）。

---

## 7. 关键风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| PPT/PDF 纯 Go 库质量不稳 | 中 | 中 | 评估 `unioffice`/`pdf` 成熟度；图片/OCR 先留接口接多模态模型；保留 Tika 作 fallback parser |
| 消息总线重构破坏现有 team_bus | 中 | 高 | 新原语只**追加**不改动现有接口；Local/Redis 双后端分别测试 |
| embed SPA 增大二进制 / 构建复杂 | 中 | 低 | SPA 独立构建，embed 可选（build tag）；保留服务端 Studio 兜底 |
| 与 Python 事件/文档格式分叉 | 低 | 高 | `tests/cross_lang` 契约测试覆盖 document/chunk/KB JSON |
| 工作量过大稀释领先项 | 中 | 高 | 每 Phase 强制含"放大既有优势"任务；P0 完成再开 P1 |

---

## 8. 验收指标（本轮）

| 指标 | 当前 | 本轮目标（Phase 5-8） |
|------|------|----------------------|
| RAG parser 数 | 1(Tika) | 4(Text/PDF/PPT/Image) |
| RAG 服务层(KB/Blob/Index) | 无 | 完整管道 + HTTP 路由 |
| RAGMiddleware | 无 | 有 |
| 消息总线原语 | 2(inbox/wakeup) | 6(+lock/registry/queue/log/projection) |
| 可嵌入 App 工厂 | 无 | CreateApp + ChatService |
| Web UI | 静态 HTML | embed SPA + Python 兼容验证 |
| Agentic Memory | 无 | 有 |
| Tracing span 属性 | 基础 | 对齐 Python 粒度 |
| 测试覆盖率 | ~60% | 65%+（新增模块带测试） |
| `go test ./... -race` | 全绿 | 全绿 |

---

## 9. 优先级与执行顺序建议

```
P0（立即）  Phase 5  RAG 托管知识库服务   ← 最大差距，最高 ROI
P1（次优先）Phase 6  消息总线 + 服务编排   ← Phase 5 的 IndexWorker 依赖
P2         Phase 7  现代 Web UI
P3         Phase 8  记忆/可观测/存储/工具

并行（始终）守住并放大 A2A/ReMe/evolver/Plugin/ONNX 领先项
```

> 建议从 **Phase 5.1-5.2（document + parser + chunker）** 起步——它们是纯 Go 库、无外部依赖、可独立验证，且是整条 RAG 管道的地基。

---

## 10. 结语

旧版方案完成了"追平 Python 2.0 的功能广度"。本轮基于 2026-07-03 真实源码发现：Python 在"托管服务化"（RAG KB / 消息总线 / 现代 UI）上完成了一次跃迁，形成了 Go 版此前未充分评估的能力带。本方案不逐行复刻 Python，而是以 Go 地道方式（纯 Go parser、`embed` SPA、interface 组合、channel 背压）补齐这一能力带，同时守住 A2A/ReMe/evolver 等独有护城河。优先级明确：**Phase 5（RAG KB）是本轮 P0**，它是"Go 版能否被当作托管 Agent 平台使用"的分水岭。
