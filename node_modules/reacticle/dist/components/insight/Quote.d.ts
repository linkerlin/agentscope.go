import type { ReactNode } from "react";
import "./insight.css";
export interface QuoteProps {
    /** The quoted text. Required. */
    children?: ReactNode;
    /** Who said it. Optional. */
    who?: string;
    /** Where it is from — work, document, link label. Optional. */
    source?: string;
}
/** A pulled quotation, optionally attributed. */
export declare function Quote({ children, who, source }: QuoteProps): import("react").JSX.Element;
//# sourceMappingURL=Quote.d.ts.map